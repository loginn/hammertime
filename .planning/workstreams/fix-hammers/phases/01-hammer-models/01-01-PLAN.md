---
phase: 01-hammer-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - models/currencies/claw_hammer.gd
  - models/currencies/forge_hammer.gd
  - models/currencies/tuning_hammer.gd
  - models/currencies/annulment_hammer.gd
  - models/currencies/alchemy_hammer.gd
  - models/currencies/divine_hammer.gd
  - scenes/forge_view.gd
  - scenes/forge_view.tscn
  - scenes/node_2d.tscn
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
requirements: [NEW-01, NEW-02, NEW-03]
must_haves:
  truths:
    - "AlchemyHammer class exists and converts Normal items to Rare with 4-6 random mods"
    - "DivineHammer class exists and rerolls mod values without changing which mods are present"
    - "AnnulmentHammer class exists and removes 1 random mod from a Magic or Rare item"
    - "scenes/forge_view.gd loads and instantiates all renamed classes without parse errors"
    - "No code references old class names ForgeHammer, ClawHammer, TuningHammer outside archive directories"
  artifacts:
    - path: "models/currencies/alchemy_hammer.gd"
      provides: "AlchemyHammer class (Normal -> Rare + 4-6 mods)"
      contains: "class_name AlchemyHammer"
    - path: "models/currencies/divine_hammer.gd"
      provides: "DivineHammer class (reroll values)"
      contains: "class_name DivineHammer"
    - path: "models/currencies/annulment_hammer.gd"
      provides: "AnnulmentHammer class (remove 1 mod)"
      contains: "class_name AnnulmentHammer"
    - path: "scenes/forge_view.gd"
      provides: "Updated @onready vars, currencies dict, signal bindings"
      contains: "AlchemyHammer.new()"
  key_links:
    - from: "scenes/forge_view.gd currencies dict"
      to: "AlchemyHammer / DivineHammer / AnnulmentHammer"
      via: ".new() instantiation"
      pattern: "AlchemyHammer\\.new\\(\\)"
    - from: "scenes/forge_view.gd @onready vars"
      to: "scenes/forge_view.tscn renamed button nodes"
      via: "$HammerSidebar/AlchemyHammerBtn etc."
      pattern: "\\$HammerSidebar/(Alchemy|Divine|Annulment)HammerBtn"
---

<objective>
Rename 3 existing mislabeled currency classes (ForgeHammer -> AlchemyHammer, TuningHammer -> DivineHammer, ClawHammer -> AnnulmentHammer) to their correct PoE names. The logic bodies are already PoE-correct; only metadata (class_name, currency_name, file paths) and every downstream reference change.

Purpose: Reconcile class names with their actual behavior so the codebase reads truthfully. This unblocks Plan 02 which adds the 3 genuinely-new classes (Augment, Chaos, Exalt) into the same `currencies` dict.
Output: 3 renamed currency files, updated forge_view.gd wiring, renamed scene tree button nodes, updated live-doc references. Project builds green after each task.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fix-hammers/STATE.md
@.planning/workstreams/fix-hammers/ROADMAP.md
@.planning/workstreams/fix-hammers/REQUIREMENTS.md
@.planning/workstreams/fix-hammers/phases/01-hammer-models/01-CONTEXT.md
@.planning/workstreams/fix-hammers/phases/01-hammer-models/01-RESEARCH.md

<interfaces>
<!-- Existing Currency template-method contract. New/renamed classes override can_apply + _do_apply only. -->

From models/currencies/currency.gd:
```gdscript
class_name Currency extends Resource
var currency_name: String
func can_apply(item: Item) -> bool: return false
func apply(item: Item) -> bool:
    if not can_apply(item): return false
    _do_apply(item)
    return true
func _do_apply(item: Item) -> void: pass
func get_error_message(item: Item) -> String: return ""
```

Current `scenes/forge_view.gd` currencies dict (lines 37-49) — this is the SINGLE source of truth mapping string keys to currency instances. Plan 01 repoints three entries (by class rename) and adds three NEW keys (alchemy/divine/annulment). Plan 02 will repoint augment/chaos/exalt to new classes:

```gdscript
var currencies: Dictionary = {
    "transmute": RunicHammer.new(),
    "augment": ForgeHammer.new(),       # PLAN 02 repoints to AugmentHammer.new()
    "alteration": TackHammer.new(),
    "regal": GrandHammer.new(),
    "chaos": ClawHammer.new(),          # PLAN 02 repoints to ChaosHammer.new()
    "exalt": TuningHammer.new(),        # PLAN 02 repoints to ExaltHammer.new()
    "fire": TagHammer.new(Tag.FIRE, "Fire Hammer"),
    ...
}
```

After Plan 01, the three mislabeled keys STILL use their old string keys (`"augment"`, `"chaos"`, `"exalt"`) because the scene button bindings still point at them. Plan 01 also ADDS three brand-new entries (`"alchemy"`, `"divine"`, `"annulment"`) instantiating the renamed classes — these have no UI button yet, which is intentional per CONTEXT.md D-10.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename 3 currency files and update metadata</name>
  <files>
    models/currencies/claw_hammer.gd (deleted via git mv),
    models/currencies/forge_hammer.gd (deleted via git mv),
    models/currencies/tuning_hammer.gd (deleted via git mv),
    models/currencies/annulment_hammer.gd (new via git mv),
    models/currencies/alchemy_hammer.gd (new via git mv),
    models/currencies/divine_hammer.gd (new via git mv)
  </files>
  <read_first>
    - models/currencies/claw_hammer.gd (current body — must remain byte-identical per D-03)
    - models/currencies/forge_hammer.gd (current body — must remain byte-identical per D-03)
    - models/currencies/tuning_hammer.gd (current body — must remain byte-identical per D-03)
    - models/currencies/currency.gd (base class contract — do NOT override apply())
    - .planning/workstreams/fix-hammers/phases/01-hammer-models/01-CONTEXT.md (decisions D-01, D-02, D-03)
  </read_first>
  <action>
    Per CONTEXT.md D-01/D-02/D-03: rename the 3 existing currency files using `git mv` and update ONLY the class_name declaration and the `currency_name` string literal inside `_init()`. All `can_apply()`, `_do_apply()`, and `get_error_message()` bodies MUST remain byte-identical — the existing logic is already PoE-correct.

    Run these 3 commands from repo root:
    1. `git mv models/currencies/claw_hammer.gd models/currencies/annulment_hammer.gd`
    2. `git mv models/currencies/forge_hammer.gd models/currencies/alchemy_hammer.gd`
    3. `git mv models/currencies/tuning_hammer.gd models/currencies/divine_hammer.gd`

    Also move the matching `.uid` files (Godot generates these; rename in lockstep):
    4. `git mv models/currencies/claw_hammer.gd.uid models/currencies/annulment_hammer.gd.uid`
    5. `git mv models/currencies/forge_hammer.gd.uid models/currencies/alchemy_hammer.gd.uid`
    6. `git mv models/currencies/tuning_hammer.gd.uid models/currencies/divine_hammer.gd.uid`

    Then edit each renamed file with EXACTLY these two-line changes:

    `models/currencies/annulment_hammer.gd`:
    - Line 1: `class_name ClawHammer extends Currency` -> `class_name AnnulmentHammer extends Currency`
    - Inside `_init()`: `currency_name = "Chaos Hammer"` -> `currency_name = "Annulment Hammer"`
    - Also update the get_error_message string: `"Item has no mods to remove"` stays — it's already generic enough, no change needed.

    `models/currencies/alchemy_hammer.gd`:
    - Line 1: `class_name ForgeHammer extends Currency` -> `class_name AlchemyHammer extends Currency`
    - Inside `_init()`: `currency_name = "Augment Hammer"` -> `currency_name = "Alchemy Hammer"`
    - Inside `get_error_message()`: `"Augment Hammer can only be used on Normal items"` -> `"Alchemy Hammer can only be used on Normal items"`

    `models/currencies/divine_hammer.gd`:
    - Line 1: `class_name TuningHammer extends Currency` -> `class_name DivineHammer extends Currency`
    - Inside `_init()`: `currency_name = "Exalt Hammer"` -> `currency_name = "Divine Hammer"`
    - The existing `get_error_message()` body does not contain a currency name string, no change needed.

    DO NOT touch `can_apply()`, `_do_apply()`, or any comments (including the historical CRAFT-05/CRAFT-06 comments — preserve them per research note). DO NOT add `print()` or logging. DO NOT introduce new helpers. DO NOT override `apply()`.
  </action>
  <acceptance_criteria>
    - `test -f models/currencies/annulment_hammer.gd && test -f models/currencies/alchemy_hammer.gd && test -f models/currencies/divine_hammer.gd` all exit 0
    - `test ! -f models/currencies/claw_hammer.gd && test ! -f models/currencies/forge_hammer.gd && test ! -f models/currencies/tuning_hammer.gd` all exit 0
    - `grep -c "^class_name AnnulmentHammer extends Currency$" models/currencies/annulment_hammer.gd` returns 1
    - `grep -c "^class_name AlchemyHammer extends Currency$" models/currencies/alchemy_hammer.gd` returns 1
    - `grep -c "^class_name DivineHammer extends Currency$" models/currencies/divine_hammer.gd` returns 1
    - `grep -c 'currency_name = "Annulment Hammer"' models/currencies/annulment_hammer.gd` returns 1
    - `grep -c 'currency_name = "Alchemy Hammer"' models/currencies/alchemy_hammer.gd` returns 1
    - `grep -c 'currency_name = "Divine Hammer"' models/currencies/divine_hammer.gd` returns 1
    - `grep -c 'currency_name = "Chaos Hammer"' models/currencies/annulment_hammer.gd` returns 0 (old string removed)
    - `grep -c 'currency_name = "Augment Hammer"' models/currencies/alchemy_hammer.gd` returns 0
    - `grep -c 'currency_name = "Exalt Hammer"' models/currencies/divine_hammer.gd` returns 0
    - `grep -c "Alchemy Hammer can only be used on Normal items" models/currencies/alchemy_hammer.gd` returns 1
    - `grep -rn "class_name ClawHammer\|class_name ForgeHammer\|class_name TuningHammer" models/` returns zero matches
    - `grep -c "func apply(" models/currencies/annulment_hammer.gd models/currencies/alchemy_hammer.gd models/currencies/divine_hammer.gd` returns 0 for each file (did NOT override apply())
  </acceptance_criteria>
  <verify>
    <automated>
    grep -q "^class_name AnnulmentHammer extends Currency$" models/currencies/annulment_hammer.gd &amp;&amp; \
    grep -q "^class_name AlchemyHammer extends Currency$" models/currencies/alchemy_hammer.gd &amp;&amp; \
    grep -q "^class_name DivineHammer extends Currency$" models/currencies/divine_hammer.gd &amp;&amp; \
    grep -q 'currency_name = "Annulment Hammer"' models/currencies/annulment_hammer.gd &amp;&amp; \
    grep -q 'currency_name = "Alchemy Hammer"' models/currencies/alchemy_hammer.gd &amp;&amp; \
    grep -q 'currency_name = "Divine Hammer"' models/currencies/divine_hammer.gd &amp;&amp; \
    ! test -f models/currencies/claw_hammer.gd &amp;&amp; \
    ! test -f models/currencies/forge_hammer.gd &amp;&amp; \
    ! test -f models/currencies/tuning_hammer.gd &amp;&amp; \
    echo "RENAME OK"
    </automated>
  </verify>
  <done>All 3 files renamed via git mv, class_name + currency_name updated, bodies byte-identical to originals, old files absent, no override of apply().</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewire forge_view.gd + rename scene tree button nodes</name>
  <files>
    scenes/forge_view.gd,
    scenes/forge_view.tscn,
    scenes/node_2d.tscn
  </files>
  <read_first>
    - scenes/forge_view.gd (full file — all 9 references to old var/class names must be updated)
    - scenes/forge_view.tscn (lines 64, 74, 136, 146, 160, 170 — button nodes + CountLabel children)
    - scenes/node_2d.tscn (lines 49, 73, 81 — alternate-scene button refs)
    - .planning/workstreams/fix-hammers/phases/01-hammer-models/01-CONTEXT.md (decisions D-07, D-08, D-09, D-10)
    - .planning/workstreams/fix-hammers/phases/01-hammer-models/01-RESEARCH.md (Pitfall 7, Pitfall 8 — `currencies` dict name, signal-bind strategy)
  </read_first>
  <action>
    Three coordinated edits so the project builds green after this task. DO NOT split across commits — scene node names, @onready paths, and dict values must land together or parse errors block testing.

    **Edit A — scenes/forge_view.gd**

    1. Update `@onready` button refs at lines 9, 12, 13:
       - Line 9: `@onready var forge_btn: Button = $HammerSidebar/ForgeHammerBtn` -> `@onready var alchemy_btn: Button = $HammerSidebar/AlchemyHammerBtn`
       - Line 12: `@onready var claw_btn: Button = $HammerSidebar/ClawHammerBtn` -> `@onready var annulment_btn: Button = $HammerSidebar/AnnulmentHammerBtn`
       - Line 13: `@onready var tuning_btn: Button = $HammerSidebar/TuningHammerBtn` -> `@onready var divine_btn: Button = $HammerSidebar/DivineHammerBtn`

    2. Update `currencies` dict at lines 37-49. Repoint the three existing entries to the RENAMED classes AND add 3 new keys. Final dict content (exact values — preserve key order by inserting new keys adjacent to their behavioral cousins):
       ```gdscript
       var currencies: Dictionary = {
           "transmute": RunicHammer.new(),
           "augment": ForgeHammer.new(),          # UNCHANGED in Plan 01 — Plan 02 repoints this to AugmentHammer.new()
           "alchemy": AlchemyHammer.new(),        # NEW key (renamed class, no UI button yet — intentional D-10)
           "alteration": TackHammer.new(),
           "regal": GrandHammer.new(),
           "chaos": ClawHammer.new(),             # UNCHANGED in Plan 01 — Plan 02 repoints this to ChaosHammer.new()
           "exalt": TuningHammer.new(),           # UNCHANGED in Plan 01 — Plan 02 repoints this to ExaltHammer.new()
           "divine": DivineHammer.new(),          # NEW key
           "annulment": AnnulmentHammer.new(),    # NEW key
           "fire": TagHammer.new(Tag.FIRE, "Fire Hammer"),
           "cold": TagHammer.new(Tag.COLD, "Cold Hammer"),
           "lightning": TagHammer.new(Tag.LIGHTNING, "Lightning Hammer"),
           "defense": TagHammer.new(Tag.DEFENSE, "Defense Hammer"),
           "physical": TagHammer.new(Tag.PHYSICAL, "Physical Hammer"),
       }
       ```
       CRITICAL: lines 39, 42, 43 keep their OLD class references (`ForgeHammer.new()`, `ClawHammer.new()`, `TuningHammer.new()`) — WAIT, no: the files have been renamed in Task 1, so `ForgeHammer`, `ClawHammer`, `TuningHammer` NO LONGER EXIST as class names. Lines 39/42/43 MUST reference the renamed classes. BUT because the `"augment"`/`"chaos"`/`"exalt"` KEY POSITIONS are still bound to UI buttons that visually display "Augment"/"Chaos"/"Exalt" (old icons/labels), leaving them pointed at the renamed classes would make the UI buttons run the wrong logic.

       Resolution (per CONTEXT.md D-10 "temporarily inconsistent" + RESEARCH.md Pitfall 8 recommended approach): **repoint the UI-bound keys to PLACEHOLDER old-behavior classes by temporarily keeping one instance**. This does not work because the old classes are gone.

       CORRECT resolution: The UI-bound keys `"augment"`, `"chaos"`, `"exalt"` in Plan 01 point to the RENAMED classes (`AlchemyHammer`, `AnnulmentHammer`, `DivineHammer`) — which is semantically wrong but is the existing behavior from the player's perspective (clicking the "Augment" button has always run what is now called AlchemyHammer). Plan 02 will fix this by introducing the real AugmentHammer/ChaosHammer/ExaltHammer classes and repointing these keys. Use this exact dict:
       ```gdscript
       var currencies: Dictionary = {
           "transmute": RunicHammer.new(),
           "augment": AlchemyHammer.new(),        # Existing behavior preserved — Plan 02 repoints to AugmentHammer.new()
           "alchemy": AlchemyHammer.new(),        # NEW key, same class, no UI button yet
           "alteration": TackHammer.new(),
           "regal": GrandHammer.new(),
           "chaos": AnnulmentHammer.new(),        # Existing behavior preserved — Plan 02 repoints to ChaosHammer.new()
           "exalt": DivineHammer.new(),           # Existing behavior preserved — Plan 02 repoints to ExaltHammer.new()
           "divine": DivineHammer.new(),          # NEW key
           "annulment": AnnulmentHammer.new(),    # NEW key
           "fire": TagHammer.new(Tag.FIRE, "Fire Hammer"),
           "cold": TagHammer.new(Tag.COLD, "Cold Hammer"),
           "lightning": TagHammer.new(Tag.LIGHTNING, "Lightning Hammer"),
           "defense": TagHammer.new(Tag.DEFENSE, "Defense Hammer"),
           "physical": TagHammer.new(Tag.PHYSICAL, "Physical Hammer"),
       }
       ```

    3. Update `currency_buttons` dict initialization at lines 99-106 — rename variable references:
       - Line 101: `"augment": forge_btn,` -> `"augment": alchemy_btn,`
       - Line 104: `"chaos": claw_btn,` -> `"chaos": annulment_btn,`
       - Line 105: `"exalt": tuning_btn` -> `"exalt": divine_btn`

    4. Update signal connect calls at lines 115, 118, 119:
       - Line 115: `forge_btn.pressed.connect(_on_currency_selected.bind("augment"))` -> `alchemy_btn.pressed.connect(_on_currency_selected.bind("augment"))`
       - Line 118: `claw_btn.pressed.connect(_on_currency_selected.bind("chaos"))` -> `annulment_btn.pressed.connect(_on_currency_selected.bind("chaos"))`
       - Line 119: `tuning_btn.pressed.connect(_on_currency_selected.bind("exalt"))` -> `divine_btn.pressed.connect(_on_currency_selected.bind("exalt"))`

    5. DO NOT touch `hammer_descriptions` dict (lines 72-84) — Phase 2 rewrites tooltips per CONTEXT.md D-10.
    6. DO NOT touch `hammer_icons` dict (lines 87-94) — PNG filenames stay per RESEARCH.md Q3.

    **Edit B — scenes/forge_view.tscn**

    Rename 3 button nodes and their CountLabel children. Use find-and-replace on the exact strings (these are unique, no risk of collateral damage):
    - `[node name="ForgeHammerBtn" type="Button" parent="HammerSidebar"]` -> `[node name="AlchemyHammerBtn" type="Button" parent="HammerSidebar"]`
    - `[node name="CountLabel" type="Label" parent="HammerSidebar/ForgeHammerBtn"]` -> `[node name="CountLabel" type="Label" parent="HammerSidebar/AlchemyHammerBtn"]`
    - `[node name="ClawHammerBtn" type="Button" parent="HammerSidebar"]` -> `[node name="AnnulmentHammerBtn" type="Button" parent="HammerSidebar"]`
    - `[node name="CountLabel" type="Label" parent="HammerSidebar/ClawHammerBtn"]` -> `[node name="CountLabel" type="Label" parent="HammerSidebar/AnnulmentHammerBtn"]`
    - `[node name="TuningHammerBtn" type="Button" parent="HammerSidebar"]` -> `[node name="DivineHammerBtn" type="Button" parent="HammerSidebar"]`
    - `[node name="CountLabel" type="Label" parent="HammerSidebar/TuningHammerBtn"]` -> `[node name="CountLabel" type="Label" parent="HammerSidebar/DivineHammerBtn"]`

    DO NOT rename `ExtResource("5_forge")`, `ExtResource("8_claw")`, `ExtResource("9_tuning")` — they are opaque IDs (RESEARCH.md line 267). DO NOT touch PNG file references.

    **Edit C — scenes/node_2d.tscn**

    Rename 3 button node references using find-and-replace (these lines have `unique_id` suffixes — preserve the full line, only change the name):
    - `[node name="ForgeHammerBtn" type="Button" parent="ButtonControl" unique_id=1049152401]` -> `[node name="AlchemyHammerBtn" type="Button" parent="ButtonControl" unique_id=1049152401]`
    - `[node name="ClawHammerBtn" type="Button" parent="ButtonControl" unique_id=1673501972]` -> `[node name="AnnulmentHammerBtn" type="Button" parent="ButtonControl" unique_id=1673501972]`
    - `[node name="TuningHammerBtn" type="Button" parent="ButtonControl" unique_id=2043849897]` -> `[node name="DivineHammerBtn" type="Button" parent="ButtonControl" unique_id=2043849897]`
  </action>
  <acceptance_criteria>
    - `grep -c "ForgeHammer\|ClawHammer\|TuningHammer" scenes/forge_view.gd` returns 0 (all class-name and btn-variable refs removed)
    - `grep -c "forge_btn\|claw_btn\|tuning_btn" scenes/forge_view.gd` returns 0
    - `grep -c "alchemy_btn" scenes/forge_view.gd` returns 4 (declaration + button_map + signal_connect + @onready path matches 1; declaration + 2 refs = 3 at minimum, allow 3-5)
    - `grep -q "@onready var alchemy_btn: Button = \$HammerSidebar/AlchemyHammerBtn" scenes/forge_view.gd` (exit 0)
    - `grep -q "@onready var annulment_btn: Button = \$HammerSidebar/AnnulmentHammerBtn" scenes/forge_view.gd` (exit 0)
    - `grep -q "@onready var divine_btn: Button = \$HammerSidebar/DivineHammerBtn" scenes/forge_view.gd` (exit 0)
    - `grep -q '"alchemy": AlchemyHammer.new()' scenes/forge_view.gd` (exit 0)
    - `grep -q '"divine": DivineHammer.new()' scenes/forge_view.gd` (exit 0)
    - `grep -q '"annulment": AnnulmentHammer.new()' scenes/forge_view.gd` (exit 0)
    - `grep -q '"augment": AlchemyHammer.new()' scenes/forge_view.gd` (exit 0 — temporary Plan 01 wiring, Plan 02 fixes)
    - `grep -q '"chaos": AnnulmentHammer.new()' scenes/forge_view.gd` (exit 0)
    - `grep -q '"exalt": DivineHammer.new()' scenes/forge_view.gd` (exit 0)
    - `grep -c "ForgeHammerBtn\|ClawHammerBtn\|TuningHammerBtn" scenes/forge_view.tscn` returns 0
    - `grep -c "AlchemyHammerBtn" scenes/forge_view.tscn` returns 2 (button node + CountLabel parent)
    - `grep -c "AnnulmentHammerBtn" scenes/forge_view.tscn` returns 2
    - `grep -c "DivineHammerBtn" scenes/forge_view.tscn` returns 2
    - `grep -c "ForgeHammerBtn\|ClawHammerBtn\|TuningHammerBtn" scenes/node_2d.tscn` returns 0
    - `grep -c "AlchemyHammerBtn\|AnnulmentHammerBtn\|DivineHammerBtn" scenes/node_2d.tscn` returns 3
    - `grep -q "unique_id=1049152401" scenes/node_2d.tscn` (exit 0 — unique_ids preserved)
    - `grep -q "unique_id=1673501972" scenes/node_2d.tscn` (exit 0)
    - `grep -q "unique_id=2043849897" scenes/node_2d.tscn` (exit 0)
    - `grep -c "hammer_descriptions" scenes/forge_view.gd` returns 1 (dict still present, unchanged per D-10)
  </acceptance_criteria>
  <verify>
    <automated>
    ! grep -q "ForgeHammer\|ClawHammer\|TuningHammer" scenes/forge_view.gd &amp;&amp; \
    ! grep -q "forge_btn\|claw_btn\|tuning_btn" scenes/forge_view.gd &amp;&amp; \
    grep -q '"alchemy": AlchemyHammer.new()' scenes/forge_view.gd &amp;&amp; \
    grep -q '"divine": DivineHammer.new()' scenes/forge_view.gd &amp;&amp; \
    grep -q '"annulment": AnnulmentHammer.new()' scenes/forge_view.gd &amp;&amp; \
    ! grep -q "ForgeHammerBtn\|ClawHammerBtn\|TuningHammerBtn" scenes/forge_view.tscn &amp;&amp; \
    ! grep -q "ForgeHammerBtn\|ClawHammerBtn\|TuningHammerBtn" scenes/node_2d.tscn &amp;&amp; \
    echo "WIRING OK"
    </automated>
  </verify>
  <done>forge_view.gd has no references to old classes or btn variables; currencies dict routes 9 base-hammer keys (3 repointed to renamed classes + 3 new keys); both scene tscn files have renamed button nodes with preserved unique_ids; hammer_descriptions and hammer_icons untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update live codebase docs to reflect renamed classes</name>
  <files>
    .planning/codebase/CONVENTIONS.md,
    .planning/codebase/ARCHITECTURE.md
  </files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (line 16 — creative-name example)
    - .planning/codebase/ARCHITECTURE.md (line 37 — currencies list example)
    - .planning/workstreams/fix-hammers/phases/01-hammer-models/01-CONTEXT.md (D-06 — naming convention note)
  </read_first>
  <action>
    Two surgical edits to live-doc references. These are **live** docs (not historical archives), so they must reflect the post-rename truth. DO NOT touch `.planning/milestones/`, `.planning/workstreams/milestone/`, `.planning/debug/resolved/`, or `.planning/quick/` — those are historical record per CONTEXT.md canonical_refs.

    **Edit A — `.planning/codebase/CONVENTIONS.md`**

    Line 16 currently reads:
    ```
    - Currency hierarchy: `Currency` -> `RunicHammer` / `ForgeHammer` / `TagHammer` etc.
    ```
    Change to:
    ```
    - Currency hierarchy: `Currency` -> `RunicHammer` / `AlchemyHammer` / `TagHammer` etc. (Base hammers use literal PoE names: Augment/Alchemy/Chaos/Exalt/Divine/Annulment. Legacy creative names remain for Runic/Tack/Grand/Tag.)
    ```

    **Edit B — `.planning/codebase/ARCHITECTURE.md`**

    Line 37 currently reads:
    ```
      - `currencies/` — Currency types (RunicHammer, ForgeHammer, etc.)
    ```
    Change to:
    ```
      - `currencies/` — Currency types (RunicHammer, AlchemyHammer, AugmentHammer, ChaosHammer, ExaltHammer, DivineHammer, AnnulmentHammer, TackHammer, GrandHammer, TagHammer)
    ```

    Also check `.planning/research/STACK.md:206` for a `ForgeHammer` reference (per RESEARCH.md line 252). If it exists, replace `ForgeHammer` with `AlchemyHammer` inline. If it does not exist (file may have changed), skip — not blocking.
  </action>
  <acceptance_criteria>
    - `grep -c "ForgeHammer" .planning/codebase/CONVENTIONS.md` returns 0
    - `grep -c "ForgeHammer" .planning/codebase/ARCHITECTURE.md` returns 0
    - `grep -q "AlchemyHammer" .planning/codebase/CONVENTIONS.md` (exit 0)
    - `grep -q "AlchemyHammer" .planning/codebase/ARCHITECTURE.md` (exit 0)
    - `grep -q "AugmentHammer\|ChaosHammer\|ExaltHammer\|DivineHammer\|AnnulmentHammer" .planning/codebase/ARCHITECTURE.md` (exit 0 — new class list present)
    - `grep -q "literal PoE names" .planning/codebase/CONVENTIONS.md` (exit 0 — naming note present)
    - Archive directories untouched: `git status .planning/milestones/ .planning/workstreams/milestone/ .planning/debug/resolved/ .planning/quick/` shows no modified files
  </acceptance_criteria>
  <verify>
    <automated>
    ! grep -q "ForgeHammer" .planning/codebase/CONVENTIONS.md &amp;&amp; \
    ! grep -q "ForgeHammer" .planning/codebase/ARCHITECTURE.md &amp;&amp; \
    grep -q "AlchemyHammer" .planning/codebase/CONVENTIONS.md &amp;&amp; \
    grep -q "AlchemyHammer" .planning/codebase/ARCHITECTURE.md &amp;&amp; \
    grep -q "literal PoE names" .planning/codebase/CONVENTIONS.md &amp;&amp; \
    echo "DOCS OK"
    </automated>
  </verify>
  <done>CONVENTIONS.md line 16 uses new class name + notes the literal PoE naming policy; ARCHITECTURE.md line 37 lists all 10 current currency classes by their post-rename names; archive directories are untouched.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run the following overall checks from repo root:

1. `grep -rn "ForgeHammer\|ClawHammer\|TuningHammer" models/ scenes/ autoloads/ tools/ .planning/codebase/ 2>/dev/null` MUST return zero matches (no references to dead class names in live code or live docs).
2. `grep -rn "forge_btn\|claw_btn\|tuning_btn" models/ scenes/ autoloads/ tools/ 2>/dev/null` MUST return zero matches.
3. Open the Godot editor (manual step for executor): confirm `scenes/forge_view.tscn` loads without red errors in the editor Output panel. Look for "parse error", "class not found", "node not found". If any error appears, read the message and fix — likely a missed rename.
4. `ls models/currencies/` should list the 10 currency files + their .uid companions, with `alchemy_hammer.gd`, `divine_hammer.gd`, `annulment_hammer.gd` present and `forge_hammer.gd`, `tuning_hammer.gd`, `claw_hammer.gd` absent.

Note: Phase 1 does NOT run the integration_test.gd suite as a Plan 01 gate (no test uses these class names per RESEARCH.md line 256). Compile/parse cleanliness in the editor is the Plan 01 validation gate.
</verification>

<success_criteria>
- All 3 renamed currency files exist with correct class_name + currency_name metadata and byte-identical bodies.
- `scenes/forge_view.gd` parses with zero references to dead class names or dead btn variables.
- `scenes/forge_view.tscn` and `scenes/node_2d.tscn` have renamed button nodes with preserved unique_ids.
- `currencies` dict has 14 entries (11 original + 3 new keys: alchemy/divine/annulment).
- Live codebase docs (CONVENTIONS.md, ARCHITECTURE.md) reflect the new class names.
- Godot editor loads the project without parse errors on `models/currencies/*.gd` or `scenes/forge_view.gd`.
- NEW-01, NEW-02, NEW-03 are architecturally satisfied (the classes exist with correct PoE behavior; behavioral validation via integration test deferred to Phase 3 per validation strategy).
</success_criteria>

<output>
After completion, create `.planning/workstreams/fix-hammers/phases/01-hammer-models/01-01-SUMMARY.md` capturing:
- Files renamed (with git-mv source -> destination pairs)
- Exact edits made to forge_view.gd, forge_view.tscn, node_2d.tscn, CONVENTIONS.md, ARCHITECTURE.md
- Confirmation that Godot editor loaded cleanly
- Any deviations from the plan (e.g., extra references found during grep audit)
- Handoff note to Plan 02: currencies dict has 14 entries; "augment"/"chaos"/"exalt" keys currently route to AlchemyHammer/AnnulmentHammer/DivineHammer and must be repointed to AugmentHammer/ChaosHammer/ExaltHammer by Plan 02.
</output>
