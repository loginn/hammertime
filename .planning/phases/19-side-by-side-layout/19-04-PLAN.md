---
phase: 19-side-by-side-layout
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [scenes/main_view.gd, scenes/main.tscn, scenes/gameplay_view.tscn, project.godot]
autonomous: true
requirements: [LAYOUT-02]
gap_closure: true

must_haves:
  truths:
    - "KEY_3 does NOT switch to settings view"
    - "TAB cycles only between forge and combat (adventure), skipping settings"
    - "Combat tab is labeled 'Adventure' in the tab bar"
    - "No misplaced 'Adventure' text label in gameplay view"
    - "No color mismatch strips at top or bottom of viewport"
  artifacts:
    - path: "scenes/main_view.gd"
      provides: "KEY_3 removed, TAB alternates forge/combat only"
    - path: "scenes/main.tscn"
      provides: "CombatTab text='Adventure', TabBar extended to y=50"
    - path: "scenes/gameplay_view.tscn"
      provides: "Title node removed"
    - path: "project.godot"
      provides: "default_clear_color set to dark gray"
  key_links:
    - from: "scenes/main_view.gd"
      to: "scenes/main.tscn"
      via: "tab button references"
      pattern: "combat_tab"
    - from: "project.godot"
      to: "scenes/main.tscn"
      via: "clear color fills gaps between nodes"
      pattern: "default_clear_color"
---

<objective>
Fix navigation shortcuts, rename Combat tab to Adventure, remove misplaced title label from gameplay view, and eliminate viewport background color strips.

Purpose: Address 3 UAT gaps (tests 2, 3, 10) so navigation works correctly and viewport has consistent coloring.
Output: Updated main_view.gd, main.tscn, gameplay_view.tscn, and project.godot.
</objective>

<execution_context>
@/home/travelboi/.claude/get-shit-done/workflows/execute-plan.md
@/home/travelboi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-side-by-side-layout/19-02-SUMMARY.md
@scenes/main_view.gd
@scenes/main.tscn
@scenes/gameplay_view.tscn
@project.godot
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix keyboard shortcuts and rename Combat tab</name>
  <files>scenes/main_view.gd, scenes/main.tscn</files>
  <action>
  **1. Update main_view.gd _input() function.**

  Remove the KEY_3 case entirely from the match statement. Update the TAB cycling to only alternate between forge and combat (skip settings):

  ```gdscript
  func _input(event) -> void:
      if event is InputEventKey and event.pressed:
          match event.keycode:
              KEY_1:
                  show_view("forge")
              KEY_2:
                  show_view("combat")
              KEY_TAB:
                  if current_view == "forge":
                      show_view("combat")
                  else:
                      show_view("forge")
  ```

  The TAB key now toggles between forge and combat only. Settings is only reachable by clicking the Settings tab button. KEY_3 is completely removed.

  **2. Update main.tscn CombatTab text.**

  Change the CombatTab button text from "Combat" to "Adventure":
  ```
  text = "Adventure"
  ```

  Also extend the TabBar ColorRect to cover the full gap between top of viewport and content area. Change TabBar offset_bottom from 40 to 50. This eliminates the 10px uncovered gap between TabBar (ended at y=40) and ContentArea (starts at y=50).
  </action>
  <verify>Read main_view.gd and confirm: (a) no KEY_3 case in match, (b) TAB only alternates forge/combat. Read main.tscn and confirm: (c) CombatTab text="Adventure", (d) TabBar offset_bottom=50.</verify>
  <done>KEY_3 removed, TAB toggles forge/combat only, tab labeled "Adventure", no gap between TabBar and ContentArea.</done>
</task>

<task type="auto">
  <name>Task 2: Remove gameplay title label and set viewport clear color</name>
  <files>scenes/gameplay_view.tscn, project.godot</files>
  <action>
  **1. Remove the Title node from gameplay_view.tscn.**

  Delete the entire Title Label node block (lines 15-23 in current file):
  ```
  [node name="Title" type="Label" parent="."]
  anchors_preset = 2
  anchor_top = 1.0
  anchor_bottom = 1.0
  offset_left = 50.0
  offset_top = -50.0
  offset_right = 200.0
  offset_bottom = -20.0
  text = "Adventure"
  horizontal_alignment = 1
  ```

  This removes the misplaced "Adventure" label that was anchored to the bottom of the gameplay viewport.

  **2. Set default_clear_color in project.godot.**

  Add a `[rendering/environment/defaults]` section (or add to existing [rendering] section) with:
  ```
  environment/defaults/default_clear_color=Color(0.1, 0.1, 0.1, 1)
  ```

  This matches the dark #1a1a1a background used by ForgeView and other scenes, so any uncovered pixels at viewport edges show the same dark color instead of Godot's default gray-blue.

  Place this line in the existing `[rendering]` section, after the `renderer/rendering_method` line.
  </action>
  <verify>Read gameplay_view.tscn and confirm no "Title" node exists. Read project.godot and confirm default_clear_color is set to Color(0.1, 0.1, 0.1, 1) in the rendering section.</verify>
  <done>No misplaced "Adventure" text in gameplay view, viewport clear color matches dark theme background.</done>
</task>

</tasks>

<verification>
1. Read main_view.gd — confirm KEY_3 is absent, TAB only toggles forge/combat
2. Read main.tscn — confirm CombatTab text="Adventure", TabBar offset_bottom=50
3. Read gameplay_view.tscn — confirm no Title node
4. Read project.godot — confirm default_clear_color=Color(0.1, 0.1, 0.1, 1)
</verification>

<success_criteria>
- Pressing KEY_3 does nothing (no settings shortcut)
- TAB alternates between forge and combat only
- Tab bar shows "Adventure" instead of "Combat"
- No "Adventure" label floating in gameplay view
- Viewport has consistent dark background with no color strips at edges
</success_criteria>

<output>
After completion, create `.planning/phases/19-side-by-side-layout/19-04-SUMMARY.md`
</output>
