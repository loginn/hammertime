---
phase: 42
plan: 01
title: "Add Tag and StatType constants for v1.8"
wave: 1
depends_on: []
files_modified:
  - autoloads/tag.gd
autonomous: true
requirements:
  - AFF-06
  - SPELL-01
  - SPELL-02
---

# Plan 42-01: Add Tag and StatType constants for v1.8

## Objective
Add all new Tag string constants (STR, DEX, INT, SPELL, CHAOS) and StatType enum entries (FLAT_SPELL_DAMAGE, INCREASED_SPELL_DAMAGE, INCREASED_CAST_SPEED, BLEED_DAMAGE, POISON_DAMAGE, BURN_DAMAGE, CHAOS_RESISTANCE) to `autoloads/tag.gd`. Zero functional changes -- pure additive constants that later phases will reference.

## must_haves
- tag.gd contains STR, DEX, INT string constants (AFF-06)
- tag.gd contains FLAT_SPELL_DAMAGE, INCREASED_SPELL_DAMAGE, INCREASED_CAST_SPEED in StatType enum (SPELL-01)
- tag.gd contains SPELL string constant (SPELL-02)
- tag.gd contains CHAOS string constant and CHAOS_RESISTANCE StatType entry (bonus, needed by Phase 48)
- tag.gd contains BLEED_DAMAGE, POISON_DAMAGE, BURN_DAMAGE StatType entries (bonus, needed by Phase 48)
- All existing StatType enum entries retain their original integer values (0-18 unchanged)
- Existing integration tests pass with zero regressions
- No functional code changes anywhere -- constants only in tag.gd

## Tasks

<task id="42-01-01">
### Add Tag string constants and StatType enum entries

**Action:** Edit `autoloads/tag.gd` to add 5 new Tag constants and 7 new StatType enum entries.

**Details:**

Add the following Tag constants after line 22 (`const EVASION = "EVASION"`), with grouping comments:

```gdscript
# Element types
const CHAOS = "CHAOS"
# Damage channels
const SPELL = "SPELL"
# Archetype identity
const STR = "STR"
const DEX = "DEX"
const INT = "INT"
```

Append the following StatType entries at the END of the enum (after `ALL_RESISTANCE,` on line 43), preserving existing integer values 0-18:

```gdscript
	# Spell damage stats
	FLAT_SPELL_DAMAGE,
	INCREASED_SPELL_DAMAGE,
	INCREASED_CAST_SPEED,
	# DoT damage stats
	BLEED_DAMAGE,
	POISON_DAMAGE,
	BURN_DAMAGE,
	# Chaos resistance
	CHAOS_RESISTANCE,
```

The final file should have 27 Tag constants and 26 StatType entries. New StatType entries get integer values 19-25.

**Requirement coverage:**
- AFF-06: Tag.STR, Tag.DEX, Tag.INT
- SPELL-01: StatType.FLAT_SPELL_DAMAGE, StatType.INCREASED_SPELL_DAMAGE, StatType.INCREASED_CAST_SPEED
- SPELL-02: Tag.SPELL
- Bonus: Tag.CHAOS, StatType.BLEED_DAMAGE, StatType.POISON_DAMAGE, StatType.BURN_DAMAGE, StatType.CHAOS_RESISTANCE

<verify>
1. Confirm tag.gd parses without syntax errors (game launches)
2. Run integration_test.tscn -- must show "ALL PASSED" with zero failures
3. Verify existing StatType integer values are unchanged: FLAT_DAMAGE=0, ALL_RESISTANCE=18
4. Verify new constants are accessible: Tag.SPELL, Tag.STR, Tag.DEX, Tag.INT, Tag.CHAOS
5. Verify new StatType entries exist: Tag.StatType.FLAT_SPELL_DAMAGE (19), Tag.StatType.INCREASED_SPELL_DAMAGE (20), Tag.StatType.INCREASED_CAST_SPEED (21), Tag.StatType.BLEED_DAMAGE (22), Tag.StatType.POISON_DAMAGE (23), Tag.StatType.BURN_DAMAGE (24), Tag.StatType.CHAOS_RESISTANCE (25)
</verify>
</task>

## Verification
- [ ] `autoloads/tag.gd` contains all 5 new Tag constants (STR, DEX, INT, SPELL, CHAOS)
- [ ] `autoloads/tag.gd` StatType enum contains all 7 new entries appended at end
- [ ] Existing StatType values 0-18 are unchanged (serialization safety)
- [ ] Integration test suite passes with zero failures
- [ ] No other files modified -- zero functional changes
- [ ] Requirements AFF-06, SPELL-01, SPELL-02 fully satisfied
