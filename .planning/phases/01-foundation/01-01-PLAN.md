---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - affix.gd
  - armor.gd
  - basic_armor.gd
  - basic_boots.gd
  - basic_helmet.gd
  - basic_ring.gd
  - boots.gd
  - crafting_view.gd
  - gameplay_view.gd
  - helmet.gd
  - hero.gd
  - hero_view.gd
  - implicit.gd
  - item_affixes.gd
  - item.gd
  - item_view.gd
  - light_sword.gd
  - main_view.gd
  - ring.gd
  - Tag.gd
  - weapon.gd
autonomous: true

must_haves:
  truths:
    - "Running gdformat --check on any .gd file exits 0 (no changes needed)"
    - "Every function signature in the codebase has a return type hint (-> Type or -> void)"
  artifacts:
    - path: "affix.gd"
      provides: "Formatted code with return type hints"
      contains: "-> void"
    - path: "item.gd"
      provides: "Formatted code with return type hints"
      contains: "-> void"
    - path: "crafting_view.gd"
      provides: "Formatted code with return type hints (18 functions)"
      contains: "-> void"
    - path: "gameplay_view.gd"
      provides: "Formatted code with return type hints (15 functions)"
      contains: "-> void"
    - path: "hero_view.gd"
      provides: "Formatted code with return type hints (13 functions)"
      contains: "-> void"
    - path: "hero.gd"
      provides: "Formatted code with return type hints (9 functions)"
      contains: "-> void"
  key_links:
    - from: "all .gd files"
      to: "gdformat"
      via: "gdformat --check . exits 0"
      pattern: "gdformat --check"
---

<objective>
Format all GDScript files with gdformat and add return type hints to every function signature.

Purpose: Establish consistent code style (STYLE-01) and complete type safety (STYLE-03) across the entire codebase before any file reorganization. Content changes must happen before file moves to keep references stable.

Output: All 21 .gd files formatted and fully type-hinted.
</objective>

<execution_context>
@/home/travelboi/.claude/get-shit-done/workflows/execute-plan.md
@/home/travelboi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install gdformat and format all GDScript files</name>
  <files>
    affix.gd, armor.gd, basic_armor.gd, basic_boots.gd, basic_helmet.gd,
    basic_ring.gd, boots.gd, crafting_view.gd, gameplay_view.gd, helmet.gd,
    hero.gd, hero_view.gd, implicit.gd, item_affixes.gd, item.gd, item_view.gd,
    light_sword.gd, main_view.gd, ring.gd, Tag.gd, weapon.gd
  </files>
  <action>
    1. Install gdformat via pipx (isolated install, avoids polluting system Python):
       ```
       pipx install "gdtoolkit==4.*"
       ```
       If pipx not available, fall back to: `pip3 install "gdtoolkit==4.*"`

    2. Verify installation: `gdformat --version`

    3. Run dry-run first to see what would change:
       ```
       gdformat --check .
       ```
       Review output to identify any files that might cause parser issues.

    4. Format all files:
       ```
       gdformat .
       ```

    5. Review changes with `git diff` to verify formatting looks correct (indentation, spacing, line breaks). Pay attention to any unexpected changes.

    6. If gdformat fails on any file (parser error), exclude that file temporarily and format the rest. Note any exclusions in the summary.

    NOTE: Do NOT commit yet -- Task 2 will also modify these files. Both tasks should be committed together.
  </action>
  <verify>
    Run `gdformat --check .` and confirm it exits with code 0 (no files need formatting).
  </verify>
  <done>All 21 .gd files pass gdformat --check with no changes needed.</done>
</task>

<task type="auto">
  <name>Task 2: Add return type hints to all function signatures</name>
  <files>
    affix.gd, armor.gd, basic_armor.gd, basic_boots.gd, basic_helmet.gd,
    basic_ring.gd, boots.gd, crafting_view.gd, gameplay_view.gd, helmet.gd,
    hero.gd, hero_view.gd, item.gd, light_sword.gd, main_view.gd, ring.gd,
    weapon.gd
  </files>
  <action>
    Add `-> Type` return type hints to every function that lacks one. There are 77 functions across 17 files missing hints.

    Rules for determining return types:
    - Functions that return nothing: `-> void`
    - `_init()` functions: `-> void`
    - `_ready()` functions: `-> void`
    - `_input(event)` functions: `-> void`
    - Signal callbacks (`_on_*`): `-> void`
    - Functions with `return some_value`: Infer type from what is returned
    - Functions that print/modify state but return nothing: `-> void`

    Files and their missing hints (by file, approximate count):

    **crafting_view.gd (18 functions):** All are void except possibly `add_item_to_inventory` -- read each function body to determine actual return type. Functions like `ImplicitHammer_toggled`, `AddPrefixHammer_toggled`, `AddSuffixHammer_toggled` are signal handlers -> void.

    **gameplay_view.gd (15 functions):** Most are void (game loop functions). Check `get_random_item_base` -- already has `-> Item`. Verify `clear_area`, `give_hammer_rewards`, `check_area_progression`, etc.

    **hero_view.gd (13 functions):** Mix of void and typed. Several already have types. Check remaining: `_ready`, `_on_item_slot_clicked`, `equip_item`, `update_slot_display`, `update_all_slots`, `update_stats_display`, `set_last_crafted_item`, `update_crafted_item_stats_display`, etc.

    **hero.gd (9 functions):** `_init`, `take_damage`, `heal`, `die`, `revive`, `equip_item`, `unequip_item`, `update_stats`, `calculate_crit_stats` -- all likely void.

    **main_view.gd (6 functions):** `_ready`, `_input`, `_on_*_pressed`, `show_view` -- all void.

    **item.gd (4 functions):** `display` -> void, `reroll_affix` -> void, `add_prefix` -> void (or bool?), `add_suffix` -> void (or bool?). Read function bodies to confirm.

    **affix.gd (2 functions):** `reroll` -> void, `display` -> void.

    **Remaining files (1 each):** `weapon.gd` (update_value -> void), `ring.gd` (update_value -> void), `boots.gd` (update_value -> void), `helmet.gd` (update_value -> void), `armor.gd` (update_value -> void), `basic_*.gd` (_init -> void), `light_sword.gd` (_init -> void).

    IMPORTANT: Read each function body before adding the type hint. Do not guess -- the return type must match what the function actually returns. If a function has multiple return paths, all must return the same type.

    After adding all hints, run gdformat again to ensure formatting consistency is maintained:
    ```
    gdformat .
    ```
  </action>
  <verify>
    1. Run: `grep -rn "^func " *.gd | grep -v "\->"` -- should return zero results (no functions without return type hints).
    2. Run: `gdformat --check .` -- should exit 0 (formatting still consistent after edits).
  </verify>
  <done>Every function signature in every .gd file has an explicit return type hint (-> Type or -> void). No function exists without a return type annotation. gdformat --check still passes.</done>
</task>

</tasks>

<verification>
1. `gdformat --check .` exits 0 -- all files formatted (STYLE-01)
2. `grep -rn "^func " *.gd | grep -v "\->"` returns empty -- all functions have return types (STYLE-03)
3. `git diff --stat` shows changes only to .gd files (no unexpected modifications)
</verification>

<success_criteria>
- All 21 .gd files pass gdformat with zero changes needed
- All 117 functions (77 previously missing + 40 already typed) have explicit return type hints
- Code is ready for file renaming and reorganization in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
