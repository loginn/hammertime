---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - project.godot
autonomous: false

must_haves:
  truths:
    - "Every .gd and .tscn file uses snake_case naming (no PascalCase files, no spaces in filenames)"
    - "Project files live in feature-based folders (models/, models/items/, models/affixes/, scenes/, autoloads/) with nothing in root except project.godot and config files"
    - "The game launches and plays identically to before the reorganization (F5 test passes)"
  artifacts:
    - path: "models/items/item.gd"
      provides: "Item base class in organized location"
    - path: "models/items/weapon.gd"
      provides: "Weapon class in organized location"
    - path: "models/items/armor.gd"
      provides: "Armor class in organized location"
    - path: "models/items/helmet.gd"
      provides: "Helmet class in organized location"
    - path: "models/items/boots.gd"
      provides: "Boots class in organized location"
    - path: "models/items/ring.gd"
      provides: "Ring class in organized location"
    - path: "models/items/light_sword.gd"
      provides: "Light sword subclass in organized location"
    - path: "models/items/basic_armor.gd"
      provides: "Basic armor subclass in organized location"
    - path: "models/items/basic_boots.gd"
      provides: "Basic boots subclass in organized location"
    - path: "models/items/basic_helmet.gd"
      provides: "Basic helmet subclass in organized location"
    - path: "models/items/basic_ring.gd"
      provides: "Basic ring subclass in organized location"
    - path: "models/affixes/affix.gd"
      provides: "Affix class in organized location"
    - path: "models/affixes/implicit.gd"
      provides: "Implicit class in organized location"
    - path: "models/hero.gd"
      provides: "Hero class in organized location"
    - path: "scenes/main.tscn"
      provides: "Main scene in organized location"
    - path: "scenes/main_view.gd"
      provides: "Main view script in organized location"
    - path: "scenes/hero_view.tscn"
      provides: "Hero view scene (renamed from 'Hero view.tscn')"
    - path: "scenes/hero_view.gd"
      provides: "Hero view script in organized location"
    - path: "scenes/gameplay_view.tscn"
      provides: "Gameplay view scene (renamed from 'Gameplay view.tscn')"
    - path: "scenes/gameplay_view.gd"
      provides: "Gameplay view script in organized location"
    - path: "scenes/crafting_view.gd"
      provides: "Crafting view script in organized location"
    - path: "scenes/item_view.gd"
      provides: "Item view script in organized location"
    - path: "scenes/node_2d.tscn"
      provides: "Node2D scene in organized location"
    - path: "autoloads/item_affixes.gd"
      provides: "ItemAffixes autoload in organized location"
    - path: "autoloads/tag.gd"
      provides: "Tag autoload (renamed from Tag.gd) in organized location"
    - path: "project.godot"
      provides: "Updated autoload paths and main scene path"
      contains: "autoloads/"
  key_links:
    - from: "project.godot [autoload]"
      to: "autoloads/item_affixes.gd"
      via: "autoload path string"
      pattern: 'ItemAffixes="\*res://autoloads/item_affixes.gd"'
    - from: "project.godot [autoload]"
      to: "autoloads/tag.gd"
      via: "autoload path string"
      pattern: 'Tag="\*res://autoloads/tag.gd"'
    - from: "project.godot [application]"
      to: "scenes/main.tscn"
      via: "main scene path"
      pattern: 'run/main_scene="res://scenes/main.tscn"'
---

<objective>
Rename files to snake_case, create feature-based folder structure, and move all files into organized folders using Godot Editor.

Purpose: Establish proper file organization (STYLE-02, ORG-01, ORG-02, ORG-03) with preserved scene references. This plan requires the user to perform file operations in Godot Editor's FileSystem dock -- terminal-based moves would break UID-based scene references.

Output: Clean folder structure with all files organized by feature, all naming conventions followed, and game fully functional.
</objective>

<execution_context>
@/home/travelboi/.claude/get-shit-done/workflows/execute-plan.md
@/home/travelboi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create folder structure and prepare move instructions</name>
  <files>project.godot</files>
  <action>
    Claude creates the empty folder directories that Godot needs. These CAN be created from terminal because they are just empty folders (no resources involved).

    Create the following folders inside the project root:
    ```
    mkdir -p models/items models/affixes scenes autoloads
    ```

    Each folder needs a `.gdkeep` or similar marker so git tracks them, OR just create them and note that Godot will populate them when files are moved in.

    NOTE: Do NOT move any files from terminal. Only create empty directories. The user will move files in Godot Editor in Task 2.

    Also, read project.godot and prepare the exact edits that will be needed AFTER moves:
    - `run/main_scene` path will change from `res://main.tscn` to `res://scenes/main.tscn`
    - Autoload paths will change (see Task 3)

    Document the complete move list for the user in a clear, ordered format that Task 2 will reference.
  </action>
  <verify>
    Run `ls -d models/ models/items/ models/affixes/ scenes/ autoloads/` and confirm all 5 directories exist.
  </verify>
  <done>Five directories exist: models/, models/items/, models/affixes/, scenes/, autoloads/. No files have been moved.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Rename and move files in Godot Editor</name>
  <files>All 21 .gd files and 4 .tscn files (moved by user in Godot Editor, not by Claude)</files>
  <action>
    The user must perform ALL file operations below in Godot Editor's FileSystem dock.
    Right-click each file -> "Move/Rename..." -> select destination.

    IMPORTANT: Commit the current state before starting moves (`git add . && git commit -m "chore: save state before file moves"`). This gives a rollback point if something breaks.

    Open Godot Editor with the Hammertime project. In the FileSystem dock (bottom-left), perform these operations IN ORDER. Test with F5 after each group.

    **GROUP 1: Rename files (fix naming violations)**

    1. Right-click `Tag.gd` -> Rename to `tag.gd` (same location, just lowercase)
    2. Right-click `Gameplay view.tscn` -> Rename to `gameplay_view.tscn` (remove space, add underscore)
    3. Right-click `Hero view.tscn` -> Rename to `hero_view.tscn` (remove space, add underscore)

    Press F5 to test. If errors, check the Output panel for broken references.

    **GROUP 2: Move model files to models/**

    4. Move `hero.gd` -> `models/hero.gd`

    **GROUP 3: Move item files to models/items/**

    5. Move `item.gd` -> `models/items/item.gd`
    6. Move `weapon.gd` -> `models/items/weapon.gd`
    7. Move `armor.gd` -> `models/items/armor.gd`
    8. Move `helmet.gd` -> `models/items/helmet.gd`
    9. Move `boots.gd` -> `models/items/boots.gd`
    10. Move `ring.gd` -> `models/items/ring.gd`
    11. Move `light_sword.gd` -> `models/items/light_sword.gd`
    12. Move `basic_armor.gd` -> `models/items/basic_armor.gd`
    13. Move `basic_boots.gd` -> `models/items/basic_boots.gd`
    14. Move `basic_helmet.gd` -> `models/items/basic_helmet.gd`
    15. Move `basic_ring.gd` -> `models/items/basic_ring.gd`

    Press F5 to test. If errors about missing files, check Output panel.

    **GROUP 4: Move affix files to models/affixes/**

    16. Move `affix.gd` -> `models/affixes/affix.gd`
    17. Move `implicit.gd` -> `models/affixes/implicit.gd`

    Press F5 to test.

    **GROUP 5: Move autoload files to autoloads/**

    18. Move `item_affixes.gd` -> `autoloads/item_affixes.gd`
    19. Move `tag.gd` (already renamed from Tag.gd) -> `autoloads/tag.gd`

    NOTE: After moving autoloads, the game WILL crash on F5. This is expected -- autoload paths in project.godot are string-based and need manual updating. Proceed to GROUP 6.

    **GROUP 6: Move scene and view files to scenes/**

    20. Move `main.tscn` -> `scenes/main.tscn`
    21. Move `main_view.gd` -> `scenes/main_view.gd`
    22. Move `hero_view.tscn` (already renamed) -> `scenes/hero_view.tscn`
    23. Move `hero_view.gd` -> `scenes/hero_view.gd`
    24. Move `gameplay_view.tscn` (already renamed) -> `scenes/gameplay_view.tscn`
    25. Move `gameplay_view.gd` -> `scenes/gameplay_view.gd`
    26. Move `crafting_view.gd` -> `scenes/crafting_view.gd`
    27. Move `item_view.gd` -> `scenes/item_view.gd`
    28. Move `node_2d.tscn` -> `scenes/node_2d.tscn`

    Do NOT press F5 yet -- project.godot needs updating first (Claude will do this in Task 3).

    When all moves are done, type "moves complete" to resume.
  </action>
  <verify>
    User confirms all 28 operations completed in Godot Editor by typing "moves complete".
  </verify>
  <done>All files renamed and moved to their target folders via Godot Editor's FileSystem dock.</done>
</task>

<task type="auto">
  <name>Task 3: Update project.godot paths and verify file structure</name>
  <files>project.godot</files>
  <action>
    After the user has moved all files in Godot Editor, update project.godot to reflect new paths:

    1. Update main scene path:
       ```ini
       # Before
       run/main_scene="res://main.tscn"
       # After
       run/main_scene="res://scenes/main.tscn"
       ```

    2. Update autoload paths:
       ```ini
       # Before
       ItemAffixes="*res://item_affixes.gd"
       Tag="*res://Tag.gd"
       # After
       ItemAffixes="*res://autoloads/item_affixes.gd"
       Tag="*res://autoloads/tag.gd"
       ```

    3. Check for any string-based `load()` or `preload()` calls in ALL .gd files that reference old paths. Search with:
       ```
       grep -rn 'load("res://' scenes/ models/ autoloads/
       grep -rn "load('res://" scenes/ models/ autoloads/
       ```
       Update any found references to use new paths.

    4. Check for any `get_node()` or `$` references that might need updating (these reference node tree, not file paths, so usually unchanged -- but verify).

    5. Verify the final folder structure matches the target:
       ```
       Root: project.godot, export_presets.cfg, icon.svg, .editorconfig (config only)
       models/: hero.gd
       models/items/: item.gd, weapon.gd, armor.gd, helmet.gd, boots.gd, ring.gd, light_sword.gd, basic_armor.gd, basic_boots.gd, basic_helmet.gd, basic_ring.gd
       models/affixes/: affix.gd, implicit.gd
       scenes/: main.tscn, main_view.gd, hero_view.tscn, hero_view.gd, gameplay_view.tscn, gameplay_view.gd, crafting_view.gd, item_view.gd, node_2d.tscn
       autoloads/: item_affixes.gd, tag.gd
       ```

    6. Verify no .gd or .tscn files remain in root:
       ```
       ls *.gd *.tscn 2>/dev/null
       ```
       Should return nothing.
  </action>
  <verify>
    1. `ls *.gd *.tscn 2>/dev/null` returns empty (no scripts/scenes in root)
    2. `ls models/items/*.gd | wc -l` returns 11
    3. `ls models/affixes/*.gd | wc -l` returns 2
    4. `ls models/hero.gd` exists
    5. `ls scenes/*.gd scenes/*.tscn | wc -l` returns 9
    6. `ls autoloads/*.gd | wc -l` returns 2
    7. `grep "autoloads/" project.godot` shows updated paths
    8. `grep "scenes/main.tscn" project.godot` shows updated main scene
  </verify>
  <done>
    project.godot has correct autoload and main scene paths. No .gd or .tscn files remain in project root. All files are in their designated feature-based folders.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify game launches and plays correctly</name>
  <files>All files in models/, scenes/, autoloads/ (verification only, no modifications)</files>
  <action>
    All automated work is complete. The user now needs to verify the game still functions after the full reorganization. This is the final gate for Phase 1.
  </action>
  <verify>
    User opens Godot Editor, presses F5, and confirms game launches without errors and all views work.
  </verify>
  <done>User has confirmed the game launches and plays identically to before reorganization.</done>
</task>

</tasks>

<verification>
1. No .gd or .tscn files in project root (ORG-02)
2. All files in correct feature folders: models/items/, models/affixes/, models/, scenes/, autoloads/ (ORG-01)
3. No PascalCase or space-containing filenames (STYLE-02): Tag.gd -> tag.gd, "Gameplay view.tscn" -> gameplay_view.tscn, "Hero view.tscn" -> hero_view.tscn
4. project.godot autoload paths point to autoloads/ folder (ORG-03)
5. Game launches and plays identically to before reorganization (ORG-03, F5 test)
</verification>

<success_criteria>
- Zero .gd/.tscn files in project root
- 25 files organized across 5 folders matching target structure
- 3 files renamed to follow snake_case convention
- Game launches with F5 and all three views (Hero, Crafting, Gameplay) function correctly
- No broken references in Godot Output panel
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
