---
phase: 26-ui-range-display
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scenes/gameplay_view.gd
autonomous: true
requirements:
  - DISP-04

must_haves:
  truths:
    - "Gameplay view displays the current pack's name during combat"
    - "Gameplay view displays the current pack's damage element type during combat"
    - "Pack info is visible when fighting and hidden when not in combat"
  artifacts:
    - path: "scenes/gameplay_view.gd"
      provides: "Pack name and element display in combat UI"
      contains: "pack_name"
  key_links:
    - from: "scenes/gameplay_view.gd update_display()"
      to: "models/monsters/monster_pack.gd"
      via: "reads pack.pack_name and pack.element for display"
      pattern: "pack_name"
---

<objective>
Add pack name and damage element type display to the gameplay view during combat.

Purpose: Players need to see which pack they're fighting and what element it attacks with. This helps them understand why some packs deal more or less damage (resistances) and adds identity to pack encounters.
Output: gameplay_view.gd with pack info display in combat UI.
</objective>

<execution_context>
@/home/travelboi/.claude/get-shit-done/workflows/execute-plan.md
@/home/travelboi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-ui-range-display/26-RESEARCH.md
@scenes/gameplay_view.gd
@scenes/gameplay_view.tscn
@models/monsters/monster_pack.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pack name and element display to gameplay view</name>
  <files>scenes/gameplay_view.gd</files>
  <action>
In `scenes/gameplay_view.gd`, add pack info display to the existing combat UI.

**1. Update the pack HP label to include pack name and element:**

In the `update_display()` function, in the section where pack HP is displayed (when `combat_engine.state == CombatEngine.State.FIGHTING` and `pack != null`), update the pack_hp_label to include the pack name and element:

Change:
```gdscript
pack_hp_label.text = "%.0f/%.0f" % [pack.hp, pack.max_hp]
```

To:
```gdscript
pack_hp_label.text = "%s (%s) — %.0f/%.0f" % [
	pack.pack_name, pack.element.capitalize(), pack.hp, pack.max_hp
]
```

This shows: "Dire Wolf (Physical) -- 150/150" or "Fire Imp (Fire) -- 200/200".

The `.capitalize()` call converts "physical" -> "Physical", "fire" -> "Fire", etc.

This is the simplest approach:
- No new UI nodes needed
- No .tscn file changes
- Pack name and element visible during combat
- Hidden automatically when combat_engine.state != FIGHTING (existing logic)

Do NOT modify any other display logic. Do NOT add new UI nodes or modify the .tscn file.
  </action>
  <verify>
1. Grep gameplay_view.gd for `pack_name` to confirm it's used in the display
2. Grep gameplay_view.gd for `pack.element` to confirm element is displayed
3. Verify `.capitalize()` is used for element name formatting
4. Verify the label text includes pack HP alongside name/element
5. Game launches without errors; pack name and element visible during combat
  </verify>
  <done>
Pack HP label now shows pack name, element type, and HP. Format: "PackName (Element) -- HP/MaxHP". Visible during combat, hidden when not fighting. No new UI nodes or .tscn changes needed.
  </done>
</task>

</tasks>

<verification>
1. Gameplay view shows pack name during combat
2. Gameplay view shows pack element type during combat
3. Pack info hidden when not in combat (existing visibility logic)
4. Label fits in viewport without overflow
</verification>

<success_criteria>
- Pack name and element visible during combat encounters
- Display format is clear and readable
- No UI overflow at 1280x720
</success_criteria>

<output>
After completion, create `.planning/phases/26-ui-range-display/26-02-SUMMARY.md`
</output>
