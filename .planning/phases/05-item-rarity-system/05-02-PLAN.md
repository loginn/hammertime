---
phase: 05-item-rarity-system
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - models/items/item.gd
  - scenes/hero_view.gd
  - scenes/gameplay_view.gd
  - scenes/crafting_view.gd
autonomous: true

must_haves:
  truths:
    - "Item name text is colored by rarity: white for Normal, blue for Magic, yellow for Rare"
    - "Equipment slot buttons in hero_view show rarity color of equipped item"
    - "Items dropped from areas are always Normal with 0 explicit mods"
    - "Items created for crafting inventory start as Normal with 0 explicit mods"
  artifacts:
    - path: "models/items/item.gd"
      provides: "get_rarity_color() method returning Color for rarity tier, rarity-colored name in get_display_text()"
      contains: "get_rarity_color"
    - path: "scenes/hero_view.gd"
      provides: "Equipment slot buttons colored by equipped item rarity"
      contains: "get_rarity_color"
    - path: "scenes/gameplay_view.gd"
      provides: "Dropped items are clean Normal (no random affixes added on drop)"
      contains: "get_random_item_base"
  key_links:
    - from: "models/items/item.gd get_display_text()"
      to: "get_rarity_color()"
      via: "name line uses rarity color"
      pattern: "get_rarity_color"
    - from: "scenes/hero_view.gd update_slot_display()"
      to: "Item.get_rarity_color()"
      via: "modulate set to item's rarity color"
      pattern: "get_rarity_color"
---

<objective>
Add visual rarity distinction (colored name text) to item displays and equipment slots, and ensure all newly created items start as clean Normal items.

Purpose: Players see rarity at a glance through color coding (white/blue/yellow). Items enter the game as Normal blanks ready for crafting.
Output: Rarity-colored item names in display text, rarity-colored equipment slot buttons, clean Normal item creation.
</objective>

<execution_context>
@/home/travelboi/.claude/get-shit-done/workflows/execute-plan.md
@/home/travelboi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-item-rarity-system/05-CONTEXT.md
@.planning/phases/05-item-rarity-system/05-01-SUMMARY.md

@models/items/item.gd
@models/items/armor.gd
@models/items/ring.gd
@models/items/helmet.gd
@models/items/boots.gd
@scenes/hero_view.gd
@scenes/gameplay_view.gd
@scenes/crafting_view.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rarity color method and update item display text</name>
  <files>models/items/item.gd</files>
  <action>
1. **Add get_rarity_color() to Item class** (models/items/item.gd). Claude picks specific hex shades per user decision. Choose colors readable on dark background:
   ```
   func get_rarity_color() -> Color:
       match rarity:
           Rarity.NORMAL:
               return Color.WHITE
           Rarity.MAGIC:
               return Color("#6888F5")   # Soft blue, readable on dark
           Rarity.RARE:
               return Color("#FFD700")   # Gold yellow
           _:
               return Color.WHITE
   ```

2. **Update get_display_text() in Item class** to use BBCode for rarity-colored name. Godot RichTextLabel supports BBCode. Since the current UI uses plain Label nodes (not RichTextLabel), use a different approach: add the rarity color information but keep the text plain. The color will be applied at the view level.

   Actually, looking at the codebase: `item_label` in crafting_view.gd is a plain `Label`, and `get_display_text()` returns a plain String. To color the name, the simplest approach is:
   - Add a `get_rarity_name_prefix()` method that returns a string like "[Normal] ", "[Magic] ", "[Rare] " — NO, user decided NO rarity prefix in the name.
   - Per user decision: "Colored name text only - no rarity prefix in the name". The color must be applied via modulate or font_color on the Label/display, not via text content.

   So instead of modifying get_display_text(), the COLOR application happens in the VIEW layer (crafting_view, hero_view). The get_rarity_color() method provides the color, and views apply it.

   Keep get_display_text() and display() unchanged in item.gd. The rarity color is purely a visual concern handled by views.

3. **No changes needed to subclass get_display_text()** in armor.gd, ring.gd, helmet.gd, boots.gd. The rarity coloring is applied by the view, not embedded in the text string.

Summary of actual changes:
- models/items/item.gd: Add `get_rarity_color() -> Color` method only
- No changes to armor.gd, ring.gd, helmet.gd, boots.gd (color applied by views)
  </action>
  <verify>
Read models/items/item.gd and confirm get_rarity_color() exists and returns correct colors for each rarity tier.
  </verify>
  <done>Item class has get_rarity_color() returning white for Normal, blue for Magic, yellow for Rare</done>
</task>

<task type="auto">
  <name>Task 2: Apply rarity colors in views and clean up item creation</name>
  <files>
    scenes/hero_view.gd
    scenes/crafting_view.gd
    scenes/gameplay_view.gd
  </files>
  <action>
**A. scenes/hero_view.gd — Equipment slot rarity colors:**

Update `update_slot_display()` to use the equipped item's rarity color instead of hardcoded `Color.WHITE`:

Current code:
```
if item:
    slot_node.text = item.item_name
    slot_node.modulate = Color.WHITE
else:
    slot_node.text = get_slot_name(slot) + "\n(Empty)"
    slot_node.modulate = Color.GRAY
```

Replace with:
```
if item:
    slot_node.text = item.item_name
    slot_node.modulate = item.get_rarity_color()
else:
    slot_node.text = get_slot_name(slot) + "\n(Empty)"
    slot_node.modulate = Color.GRAY
```

This fulfills the user decision: "Equipment slot buttons in hero_view also use rarity color based on equipped item's rarity."

Also update `get_item_stats_text()` to include rarity information in the hover display. Add rarity tier name after the item name:
```
var rarity_name = "Normal"
match item.rarity:
    Item.Rarity.MAGIC:
        rarity_name = "Magic"
    Item.Rarity.RARE:
        rarity_name = "Rare"
var stats_text = item.item_name + " (" + rarity_name + ")" + "\n\n"
```

**B. scenes/crafting_view.gd — Crafting label rarity color:**

In `update_label()`, after setting `item_label.text`, apply the rarity color to the label:
```
func update_label() -> void:
    if current_item != null:
        item_label.text = self.current_item.get_display_text()
        item_label.modulate = current_item.get_rarity_color()
    else:
        item_label.text = "No item selected for crafting"
        item_label.modulate = Color.WHITE
```

Also update `update_inventory_display()` to show rarity instead of tier (since rarity is now the primary item characteristic). Change the tier display line to show rarity name:
```
# In the display loop, replace:
#   display_text += " (Tier " + str(tier) + ")"
# With:
var rarity_name = "Normal"
match item.rarity:
    Item.Rarity.MAGIC:
        rarity_name = "Magic"
    Item.Rarity.RARE:
        rarity_name = "Rare"
display_text += " (" + rarity_name + ")"
```

**C. scenes/gameplay_view.gd — Clean Normal drops:**

The `get_random_item_base()` function already creates clean items via `random_type.new()`, and since Plan 01 sets all base types to default Normal rarity with 0 mods, no changes are needed here. The items already come out as Normal with only their implicit.

Verify that `get_random_item_base()` does NOT add any random affixes after creation. Looking at the current code: it just calls `.new()` and returns — this is already correct. No changes needed to gameplay_view.gd.

**D. scenes/crafting_view.gd — Verify starting items are clean Normal:**

In `_ready()`, the starting items are created via `.new()` which will now default to Normal rarity. The old hammer system (implicit/prefix/suffix buttons) still exists and will work for testing but will be replaced in Phase 6/8. No changes needed to the item creation in _ready().

Summary of actual file changes:
- scenes/hero_view.gd: update_slot_display() uses get_rarity_color(), get_item_stats_text() shows rarity name
- scenes/crafting_view.gd: update_label() applies rarity color to label, update_inventory_display() shows rarity name instead of tier
- scenes/gameplay_view.gd: No changes needed (already creates clean Normal items)
  </action>
  <verify>
1. Read scenes/hero_view.gd — confirm update_slot_display() uses `item.get_rarity_color()` instead of `Color.WHITE`
2. Read scenes/crafting_view.gd — confirm update_label() applies rarity color to item_label
3. Read scenes/crafting_view.gd — confirm update_inventory_display() shows rarity name
4. Read scenes/gameplay_view.gd — confirm get_random_item_base() creates clean items without adding random affixes
5. Grep for "Color.WHITE" in hero_view.gd — should NOT appear in update_slot_display (replaced by get_rarity_color)
  </verify>
  <done>Equipment slots show rarity color, crafting view shows rarity-colored item text and rarity names in inventory, all dropped items are clean Normal items</done>
</task>

</tasks>

<verification>
1. Read models/items/item.gd — confirm get_rarity_color() method exists with white/blue/yellow colors
2. Read scenes/hero_view.gd — confirm update_slot_display uses get_rarity_color(), get_item_stats_text shows rarity
3. Read scenes/crafting_view.gd — confirm update_label uses rarity color, inventory shows rarity name
4. Grep `Color.WHITE` in hero_view.gd update_slot_display — should NOT appear (replaced)
5. Grep `get_rarity_color` — should appear in item.gd, hero_view.gd, crafting_view.gd
6. Verify gameplay_view.gd get_random_item_base() creates clean Normal items (no affix additions)
</verification>

<success_criteria>
- Item.get_rarity_color() returns white for Normal, blue for Magic, yellow for Rare
- Equipment slot buttons in hero_view are colored by equipped item's rarity
- Crafting view item label is colored by current item's rarity
- Crafting inventory display shows rarity name (Normal/Magic/Rare) per item
- Item hover stats in hero_view show rarity name
- Dropped items from gameplay_view are clean Normal items (0 explicit mods, implicit only)
- No rarity prefix in item names (just colored text, per user decision)
</success_criteria>

<output>
After completion, create `.planning/phases/05-item-rarity-system/05-02-SUMMARY.md`
</output>
