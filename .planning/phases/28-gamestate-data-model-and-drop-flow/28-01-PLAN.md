---
phase: 28-gamestate-data-model-and-drop-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - autoloads/game_state.gd
  - autoloads/save_manager.gd
  - scenes/forge_view.gd
autonomous: true
requirements:
  - INV-01
  - INV-02

must_haves:
  truths:
    - "Killing a pack that drops a weapon adds the weapon to the weapon slot array"
    - "Dropping an 11th item into a full slot (10 items) is silently discarded and slot remains at 10"
    - "Starting a new game grants the starter weapon into the weapon slot array"
    - "The crafting_bench_item field is removed from GameState and no call site references it"
  artifacts:
    - path: "autoloads/game_state.gd"
      provides: "Array-based crafting_inventory, no crafting_bench_item"
      contains: "crafting_inventory"
    - path: "autoloads/save_manager.gd"
      provides: "Array-to-array save/restore (no bridge)"
      contains: "_restore_state"
    - path: "scenes/forge_view.gd"
      provides: "Array-aware add_item_to_inventory with 10-item cap"
      contains: "add_item_to_inventory"
  key_links:
    - from: "scenes/forge_view.gd"
      to: "autoloads/game_state.gd"
      via: "GameState.crafting_inventory[slot].append(item)"
      pattern: "crafting_inventory.*append"
    - from: "autoloads/save_manager.gd"
      to: "autoloads/game_state.gd"
      via: "_restore_state populates arrays into GameState.crafting_inventory"
      pattern: "crafting_inventory.*=.*items_array"
---

<objective>
Reshape GameState.crafting_inventory from single-item-per-slot to per-slot arrays, enforce a 10-item cap at the single add point, remove crafting_bench_item from GameState, complete the SaveManager bridge, and update ForgeView to handle arrays.

Purpose: Enable per-slot inventory storage so items accumulate until the slot is full, replacing the old "only keep best" behavior.
Output: Three modified files with array-based inventory model working end-to-end.
</objective>

<execution_context>
@/home/travelboi/.claude/get-shit-done/workflows/execute-plan.md
@/home/travelboi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-save-format-migration/27-01-SUMMARY.md
@autoloads/game_state.gd
@autoloads/save_manager.gd
@scenes/forge_view.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape GameState and SaveManager for array-based inventory</name>
  <files>autoloads/game_state.gd, autoloads/save_manager.gd</files>
  <action>
  **game_state.gd changes:**

  1. Delete `var crafting_bench_item: Item = null` (line 10)
  2. In `initialize_fresh_game()`:
     - Change `crafting_inventory` initialization from `{slot: null}` to `{slot: []}` for all five slots
     - Create the starter weapon and place it in the weapon array: `crafting_inventory["weapon"] = [LightSword.new()]`
     - Delete `crafting_bench_item = null` (line 69)

  **save_manager.gd changes:**

  3. Update `_build_save_data()` (lines 84-90): Instead of wrapping single items in arrays, iterate the actual arrays in GameState:
     ```
     for type_name in GameState.crafting_inventory:
         var slot_array: Array = GameState.crafting_inventory[type_name]
         var items_data: Array = []
         for item in slot_array:
             items_data.append(item.to_dict())
         crafting_inv[type_name] = items_data
     ```

  4. Update `_restore_state()` (lines 124-135): Instead of extracting first item, populate full arrays:
     ```
     for slot_name in ["weapon", "helmet", "armor", "boots", "ring"]:
         var slot_data = saved_crafting.get(slot_name, [])
         var items_array: Array = []
         if slot_data is Array:
             for item_data in slot_data:
                 if item_data is Dictionary:
                     var item = Item.create_from_dict(item_data)
                     if item != null:
                         items_array.append(item)
         GameState.crafting_inventory[slot_name] = items_array
     ```

  The Phase 27 migration code (`_migrate_v1_to_v2`) and save format version remain unchanged.
  </action>
  <verify>
  - grep for `crafting_bench_item` in game_state.gd returns zero results
  - grep for `= null` in the crafting_inventory initialization block returns zero results
  - `_build_save_data` iterates arrays, not single items
  - `_restore_state` populates arrays, not single items
  </verify>
  <done>
  GameState.crafting_inventory holds arrays for all five slots. crafting_bench_item is removed. SaveManager reads/writes actual arrays with no bridge pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ForgeView drop flow and array compatibility</name>
  <files>scenes/forge_view.gd</files>
  <action>
  **Drop flow changes:**

  1. Rewrite `add_item_to_inventory()` (lines 420-434):
     - Remove the `is_item_better()` guard entirely
     - Get the slot array: `var slot_array: Array = GameState.crafting_inventory[item_type]`
     - Check capacity: `if slot_array.size() >= 10: return` (silently discard)
     - Append: `slot_array.append(item)`
     - Call `update_inventory_display()`
     - Keep `is_item_better()` function definition (lines 465-472) untouched -- it may be needed by Phase 29 bench selection

  **ForgeView _ready() compatibility (lines 134-167):**

  2. Change the "has saved items" check (lines 135-139): Instead of `!= null`, check for non-empty arrays:
     ```
     for type_name in inventory_types:
         if not GameState.crafting_inventory[type_name].is_empty():
             has_saved_items = true
             break
     ```

  3. Starting item creation (lines 143-145): Already calls `add_item_to_inventory(starting_weapon)` which now appends. No change needed here since GameState.initialize_fresh_game already places the starter weapon.
     - Actually, since `initialize_fresh_game()` now creates the starter weapon directly, ForgeView's starting item check will find it. Remove the ForgeView starting weapon creation entirely -- GameState handles it now.

  4. Bench type selection (lines 148-159): Change `!= null` checks to `.is_empty()` checks, and read `[0]` instead of direct Item:
     ```
     if not GameState.crafting_inventory[selected_type].is_empty():
         current_item = GameState.crafting_inventory[selected_type][0]
     else:
         current_item = null
         for type_name in inventory_types:
             if not GameState.crafting_inventory[type_name].is_empty():
                 selected_type = type_name
                 current_item = GameState.crafting_inventory[type_name][0]
                 break
     ```

  **Other ForgeView consumer sites:**

  5. `_on_item_type_selected()` (line 271): Change `== null` to `.is_empty()`:
     ```
     if GameState.crafting_inventory[item_type].is_empty():
         print("No ", item_type, " in inventory - selection ignored")
         return
     ```

  6. `update_current_item()` (lines 300-310): Change null check to empty check, read `[0]`:
     ```
     if selected_type != "" and not GameState.crafting_inventory[selected_type].is_empty():
         current_item = GameState.crafting_inventory[selected_type][0]
     ```

  7. `_on_melt_pressed()` (line 354): Change `= null` to remove item from array:
     ```
     if slot_name != "None":
         var slot_array: Array = GameState.crafting_inventory[slot_name]
         var idx = slot_array.find(current_item)
         if idx >= 0:
             slot_array.remove_at(idx)
     current_item = null
     ```

  8. `_on_equip_pressed()` (line 395): Same pattern -- remove from array instead of setting null:
     ```
     var slot_array: Array = GameState.crafting_inventory[slot_name]
     var idx = slot_array.find(current_item)
     if idx >= 0:
         slot_array.remove_at(idx)
     current_item = null
     ```

  9. `update_inventory_display()` (line 485): Change to read first item from array:
     ```
     var slot_array: Array = GameState.crafting_inventory.get(item_type, [])
     var item: Item = slot_array[0] if not slot_array.is_empty() else null
     ```
  </action>
  <verify>
  - grep for `is_item_better` in add_item_to_inventory function returns zero results (call removed, function definition remains)
  - grep for `crafting_inventory\[.*\] = null` in forge_view.gd returns zero results (replaced with array removal)
  - grep for `crafting_inventory.get.*!= null` in forge_view.gd returns zero results (replaced with .is_empty())
  - All ForgeView consumer sites use array access patterns (.is_empty(), [0], .append, .remove_at)
  </verify>
  <done>
  ForgeView drop flow appends to arrays with 10-item cap. All ForgeView consumers read arrays correctly. Melt and equip remove items from arrays. No null-slot patterns remain in forge_view.gd.
  </done>
</task>

</tasks>

<verification>
1. Confirm `crafting_bench_item` does not exist in game_state.gd (grep)
2. Confirm all five slots initialize as empty arrays in `initialize_fresh_game()`
3. Confirm starter weapon is placed in weapon array
4. Confirm `add_item_to_inventory` uses append with size >= 10 guard
5. Confirm `_build_save_data` iterates arrays (not wrapping single items)
6. Confirm `_restore_state` populates arrays (not extracting first item)
7. Confirm no `crafting_inventory[slot] = null` patterns remain in forge_view.gd
8. Confirm `is_item_better()` function definition still exists but is not called in `add_item_to_inventory`
</verification>

<success_criteria>
- GameState.crafting_inventory holds Array per slot (never null, never single Item)
- 10-item cap enforced at single add point
- Save/load round-trips full arrays
- crafting_bench_item fully removed from GameState
- ForgeView works with arrays (melt, equip, display, type selection)
- is_item_better() function preserved for future use
</success_criteria>

<output>
After completion, create `.planning/phases/28-gamestate-data-model-and-drop-flow/28-01-SUMMARY.md`
</output>
