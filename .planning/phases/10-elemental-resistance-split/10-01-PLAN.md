---
phase: 10-elemental-resistance-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - autoloads/tag.gd
  - autoloads/item_affixes.gd
  - models/hero.gd
  - models/items/light_sword.gd
  - models/items/basic_ring.gd
  - scenes/hero_view.gd
autonomous: true

must_haves:
  truths:
    - "User can apply Forge Hammer and see fire/cold/lightning resistance suffixes roll on items"
    - "User can apply Forge Hammer and see all-resistance suffix roll (rarer than single-element)"
    - "User sees resistance values on item tooltip suffixes section"
    - "User sees resistance totals in Hero View defense section"
    - "User never sees 'Elemental Reduction' suffix on newly crafted items"
    - "Weapons and rings can roll resistance suffixes alongside their offensive suffixes"
  artifacts:
    - path: "autoloads/tag.gd"
      provides: "FIRE_RESISTANCE, COLD_RESISTANCE, LIGHTNING_RESISTANCE, ALL_RESISTANCE StatType enums"
      contains: "FIRE_RESISTANCE"
    - path: "autoloads/item_affixes.gd"
      provides: "4 resistance suffix definitions, no Elemental Reduction"
      contains: "Fire Resistance"
    - path: "models/hero.gd"
      provides: "Resistance aggregation from all equipment slots, getter methods"
      contains: "total_fire_resistance"
    - path: "scenes/hero_view.gd"
      provides: "Resistance display in defense section"
      contains: "Fire Resistance"
  key_links:
    - from: "autoloads/item_affixes.gd"
      to: "autoloads/tag.gd"
      via: "StatType enum references in suffix definitions"
      pattern: "Tag\\.StatType\\.FIRE_RESISTANCE"
    - from: "models/hero.gd"
      to: "autoloads/tag.gd"
      via: "StatType checks in suffix aggregation loop"
      pattern: "Tag\\.StatType\\.ALL_RESISTANCE"
    - from: "scenes/hero_view.gd"
      to: "models/hero.gd"
      via: "Getter method calls for resistance totals"
      pattern: "get_total_fire_resistance"
    - from: "models/items/light_sword.gd"
      to: "autoloads/tag.gd"
      via: "Tag.DEFENSE added to valid_tags enabling resistance suffix rolling"
      pattern: "Tag\\.DEFENSE"
---

<objective>
Implement elemental resistance split: replace generic "Elemental Reduction" suffix with individual fire, cold, lightning resistance suffixes plus all-resistance option. Add resistance aggregation to Hero model and display in Hero View.

Purpose: Players can craft items with specific elemental resistances for granular defense control, following established ARPG patterns (Path of Exile, Diablo 3). All-resistance provides space-efficient alternative at rarer tier range.

Output: 4 new resistance suffixes in affix pool, resistance aggregation in Hero model across all equipment slots, resistance display in Hero View defense section, all item types can roll resistance suffixes.
</objective>

<execution_context>
@/home/travelboi/.claude/get-shit-done/workflows/execute-plan.md
@/home/travelboi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-elemental-resistance-split/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resistance data layer (enums, suffixes, hero aggregation, item tags)</name>
  <files>
    autoloads/tag.gd
    autoloads/item_affixes.gd
    models/hero.gd
    models/items/light_sword.gd
    models/items/basic_ring.gd
  </files>
  <action>
1. **Extend Tag.StatType enum** in `autoloads/tag.gd` (after FLAT_EVASION at line 39):
   Add 4 new enum values:
   - FIRE_RESISTANCE
   - COLD_RESISTANCE
   - LIGHTNING_RESISTANCE
   - ALL_RESISTANCE

2. **Replace Elemental Reduction with 4 resistance suffixes** in `autoloads/item_affixes.gd`:
   - DELETE the "Elemental Reduction" suffix (line 142: `Affix.new("Elemental Reduction", Affix.AffixType.SUFFIX, 2, 10, [Tag.DEFENSE], [])`)
   - ADD 3 individual resistance suffixes with base_min=5, base_max=12, tags=[Tag.DEFENSE], tier_range=Vector2i(1, 8):
     - "Fire Resistance" with stat_types=[Tag.StatType.FIRE_RESISTANCE]
     - "Cold Resistance" with stat_types=[Tag.StatType.COLD_RESISTANCE]
     - "Lightning Resistance" with stat_types=[Tag.StatType.LIGHTNING_RESISTANCE]
   - ADD 1 all-resistance suffix with base_min=3, base_max=8, tags=[Tag.DEFENSE], tier_range=Vector2i(1, 5):
     - "All Resistances" with stat_types=[Tag.StatType.ALL_RESISTANCE]
   - Use full multi-line Affix.new() constructor format matching existing defensive prefixes style (lines 39-91)

3. **Enable resistance suffixes on weapons and rings** (ERES-03):
   - In `models/items/light_sword.gd`: Add `Tag.DEFENSE` to valid_tags array (currently `[Tag.PHYSICAL, Tag.ATTACK, Tag.CRITICAL]`)
   - In `models/items/basic_ring.gd`: Add `Tag.DEFENSE` to valid_tags array (currently `[Tag.ATTACK, Tag.CRITICAL, Tag.SPEED, Tag.WEAPON]`)
   - This allows has_valid_tag() to match resistance suffixes (tagged [Tag.DEFENSE]) for all item types

4. **Add resistance properties and aggregation to Hero model** in `models/hero.gd`:
   - Add 3 class properties after total_energy_shield (line 14):
     - `var total_fire_resistance: int = 0`
     - `var total_cold_resistance: int = 0`
     - `var total_lightning_resistance: int = 0`
   - Extend calculate_defense() method:
     - Initialize resistance totals to 0
     - Change equipment loop from `["helmet", "armor", "boots"]` to `["helmet", "armor", "boots", "weapon", "ring"]` (resistances come from suffixes on ALL item types)
     - After existing base_armor/evasion/ES aggregation, add suffix loop for each item:
       ```
       for suffix in item.suffixes:
           if Tag.StatType.FIRE_RESISTANCE in suffix.stat_types:
               total_fire_resistance += suffix.value
           if Tag.StatType.COLD_RESISTANCE in suffix.stat_types:
               total_cold_resistance += suffix.value
           if Tag.StatType.LIGHTNING_RESISTANCE in suffix.stat_types:
               total_lightning_resistance += suffix.value
           if Tag.StatType.ALL_RESISTANCE in suffix.stat_types:
               total_fire_resistance += suffix.value
               total_cold_resistance += suffix.value
               total_lightning_resistance += suffix.value
       ```
     - CRITICAL: Single loop per item, each suffix counted once. All-resistance adds to all three in same check block (avoids Pitfall 1: double-counting).
     - Keep existing base_armor/evasion/ES logic for helmet/armor/boots only (use `if "base_armor" in armor_item:` guard). Suffix resistance loop runs for ALL 5 slots.
   - Add 3 getter methods after get_total_energy_shield() (line 183):
     - `func get_total_fire_resistance() -> int:` returns total_fire_resistance
     - `func get_total_cold_resistance() -> int:` returns total_cold_resistance
     - `func get_total_lightning_resistance() -> int:` returns total_lightning_resistance
  </action>
  <verify>
    1. Grep autoloads/tag.gd for FIRE_RESISTANCE, COLD_RESISTANCE, LIGHTNING_RESISTANCE, ALL_RESISTANCE
    2. Grep autoloads/item_affixes.gd for "Fire Resistance", "Cold Resistance", "Lightning Resistance", "All Resistances" - should find all 4
    3. Grep autoloads/item_affixes.gd for "Elemental Reduction" - should find ZERO matches
    4. Grep models/items/light_sword.gd for "Tag.DEFENSE" - should be in valid_tags
    5. Grep models/items/basic_ring.gd for "Tag.DEFENSE" - should be in valid_tags
    6. Grep models/hero.gd for "total_fire_resistance" - should find property, calculation, and getter
    7. Verify calculate_defense loops all 5 slots: grep for '"weapon", "ring"' in hero.gd
  </verify>
  <done>
    - Tag.StatType has 4 new resistance enum values
    - ItemAffixes.suffixes has 4 resistance suffixes (fire/cold/lightning individual + all-resistance)
    - "Elemental Reduction" suffix is completely removed
    - All-resistance uses narrower tier_range Vector2i(1, 5) vs individual Vector2i(1, 8)
    - LightSword and BasicRing valid_tags include Tag.DEFENSE
    - Hero.calculate_defense() aggregates resistances from suffixes across all 5 equipment slots
    - Hero has getter methods for fire/cold/lightning resistance totals
  </done>
</task>

<task type="auto">
  <name>Task 2: Display resistance totals in Hero View defense section</name>
  <files>scenes/hero_view.gd</files>
  <action>
1. **Extend update_stats_display()** in `scenes/hero_view.gd` (lines 191-219):
   - After the existing Energy Shield display block (line 216-217), before the `if not has_defense:` check (line 218):
   - Add resistance display using same pattern as armor/evasion/ES (only show non-zero):
     ```
     var total_fire_res = GameState.hero.get_total_fire_resistance()
     var total_cold_res = GameState.hero.get_total_cold_resistance()
     var total_lightning_res = GameState.hero.get_total_lightning_resistance()

     if total_fire_res > 0:
         stats_label.text += "Fire Resistance: %d\n" % total_fire_res
         has_defense = true
     if total_cold_res > 0:
         stats_label.text += "Cold Resistance: %d\n" % total_cold_res
         has_defense = true
     if total_lightning_res > 0:
         stats_label.text += "Lightning Resistance: %d\n" % total_lightning_res
         has_defense = true
     ```
   - Display order: Armor/Evasion/ES first (base defenses), then Fire/Cold/Lightning resistances (affix-driven stats). This matches logical grouping: physical defense then elemental defense.
   - NOTE: Item tooltip display already works automatically. The get_item_stats_text() method shows all suffixes by name:value (e.g., "Fire Resistance: 47"). No changes needed to item stats display.
  </action>
  <verify>
    1. Grep scenes/hero_view.gd for "Fire Resistance" - should find display line
    2. Grep scenes/hero_view.gd for "get_total_fire_resistance" - should find getter call
    3. Verify display is inside update_stats_display() function, after Energy Shield block and before "No defense equipped" fallback
  </verify>
  <done>
    - Hero View defense section shows Fire Resistance, Cold Resistance, Lightning Resistance totals (non-zero only)
    - Resistances display below Armor/Evasion/Energy Shield
    - "(No defense equipped)" still shows when no defense stats at all
    - Item tooltip suffixes section automatically shows resistance affix names and values
  </done>
</task>

</tasks>

<verification>
1. **ERES-01 (Individual resistances replace generic):** Grep item_affixes.gd confirms "Elemental Reduction" removed and "Fire Resistance"/"Cold Resistance"/"Lightning Resistance" present with Tag.StatType references
2. **ERES-02 (All-resistance available):** Grep item_affixes.gd confirms "All Resistances" suffix with tier_range Vector2i(1, 5)
3. **ERES-03 (All item types):** All 5 item base types have Tag.DEFENSE in valid_tags (armor/boots/helmet have it natively, light_sword and basic_ring get it added)
4. **Hero aggregation:** Hero.calculate_defense() loops all 5 equipment slots, aggregates fire/cold/lightning from individual + all-resistance suffixes
5. **Hero View display:** update_stats_display() shows non-zero resistance totals in defense section
6. **No double-counting:** Single suffix loop per item, all-resistance adds to all three in one check block
</verification>

<success_criteria>
1. Fire/Cold/Lightning Resistance and All Resistances suffixes appear in forge hammer rolls
2. "Elemental Reduction" never appears on new items
3. Resistance suffixes can roll on weapons and rings (not just armor pieces)
4. All-resistance has narrower tier range (T1-T5) vs individual (T1-T8)
5. Hero View shows total fire/cold/lightning resistance when items with resistance suffixes are equipped
6. All-resistance adds to each individual resistance total (not displayed separately)
</success_criteria>

<output>
After completion, create `.planning/phases/10-elemental-resistance-split/10-01-SUMMARY.md`
</output>
