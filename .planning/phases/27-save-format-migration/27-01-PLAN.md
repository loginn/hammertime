---
phase: 27-save-format-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - autoloads/save_manager.gd
autonomous: true
requirements:
  - SAVE-01

must_haves:
  truths:
    - "Loading a hand-crafted v1 save produces per-slot arrays with the correct item count in each slot"
    - "Loading a v2 save round-trips all items in all slots without loss or duplication"
    - "The orphaned crafting_bench_item key is absent from both written saves and migrated saves"
    - "A fresh game save (version 2) loads back to empty arrays for all five slots"
  artifacts:
    - path: "autoloads/save_manager.gd"
      provides: "v1-to-v2 migration, array-based save/restore, orphaned key removal"
      contains: "_migrate_v1_to_v2"
  key_links:
    - from: "autoloads/save_manager.gd:_migrate_save"
      to: "autoloads/save_manager.gd:_migrate_v1_to_v2"
      via: "version check calling migration function"
      pattern: "if saved_version < 2"
    - from: "autoloads/save_manager.gd:_build_save_data"
      to: "GameState.crafting_inventory"
      via: "wrapping single items into arrays for save format"
      pattern: "\\[item\\.to_dict\\(\\)\\]"
    - from: "autoloads/save_manager.gd:_restore_state"
      to: "GameState.crafting_inventory"
      via: "extracting first item from saved arrays into single-item GameState slots"
      pattern: "slot_data\\[0\\]"
---

<objective>
Bump save format to v2 with per-slot inventory arrays and v1 migration.

Purpose: Enable the save format to store arrays of items per crafting slot (up to 10), preparing the persistence layer for the inventory rework in Phases 28-30. Existing v1 saves migrate automatically. The orphaned `crafting_bench_item` key is removed from the save path.

Output: Updated `autoloads/save_manager.gd` with SAVE_VERSION=2, `_migrate_v1_to_v2()`, array-based `_build_save_data()`, bridging `_restore_state()`, and no `crafting_bench_item` in saves.
</objective>

<execution_context>
@/home/travelboi/.claude/get-shit-done/workflows/execute-plan.md
@/home/travelboi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-save-format-migration/27-RESEARCH.md
@autoloads/save_manager.gd
@autoloads/game_state.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement v1-to-v2 migration and bump save version</name>
  <files>autoloads/save_manager.gd</files>
  <action>
In `autoloads/save_manager.gd`:

1. Change `const SAVE_VERSION = 1` to `const SAVE_VERSION = 2`.

2. Add a new function `_migrate_v1_to_v2(data: Dictionary) -> Dictionary` that:
   - Gets the old `crafting_inventory` dict from data (default empty dict)
   - Creates a new dict where each of the 5 slots (`weapon`, `helmet`, `armor`, `boots`, `ring`) is wrapped:
     - If old value is a non-null Dictionary: wrap in a 1-element array `[item_data]`
     - If old value is null or missing: set to empty array `[]`
   - Replaces `data["crafting_inventory"]` with the new dict
   - Erases `crafting_bench_item` from data (`data.erase("crafting_bench_item")`)
   - Returns the modified data

3. In `_migrate_save()`, uncomment/replace the placeholder to call `_migrate_v1_to_v2(data)` when `saved_version < 2`.

IMPORTANT: The five slot names are `weapon`, `helmet`, `armor`, `boots`, `ring` (matching game_state.gd line 62-68). The CONTEXT.md mentions "chest" and "gloves" but those are not the actual code names.
  </action>
  <verify>
Read the updated file and confirm:
- SAVE_VERSION is 2
- `_migrate_v1_to_v2` function exists with correct slot names
- `_migrate_save` calls `_migrate_v1_to_v2` when `saved_version < 2`
- `data.erase("crafting_bench_item")` is present in migration
  </verify>
  <done>
`_migrate_v1_to_v2()` wraps v1 single-item slots into arrays, strips `crafting_bench_item`, and `_migrate_save()` routes v1 saves through it. SAVE_VERSION is 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update _build_save_data and _restore_state for v2 array format</name>
  <files>autoloads/save_manager.gd</files>
  <action>
In `autoloads/save_manager.gd`:

1. Update `_build_save_data()` crafting inventory section:
   - For each slot in `GameState.crafting_inventory`:
     - If item is not null: serialize as `[item.to_dict()]` (1-element array)
     - If item is null: serialize as `[]` (empty array)
   - Remove `crafting_bench_item` from the returned dictionary entirely. Delete the `bench_item_data` variable, the null check, and the `"crafting_bench_item": bench_item_data` entry from the return dict.

2. Update `_restore_state()` crafting inventory section:
   - For each of the 5 slots in `["weapon", "helmet", "armor", "boots", "ring"]`:
     - Get slot data with `saved_crafting.get(slot_name, [])` (default empty array)
     - If slot_data is an Array and not empty, take `slot_data[0]` and deserialize with `Item.create_from_dict()` into `GameState.crafting_inventory[slot_name]`
     - If empty or invalid, set `GameState.crafting_inventory[slot_name] = null`
   - CRITICAL BRIDGE: GameState.crafting_inventory still holds single Items (not arrays) in Phase 27. The array format lives in the save file only. Phase 28 will change GameState to hold arrays.
   - Remove the `crafting_bench_item` restoration block entirely (the `var bench_data = data.get("crafting_bench_item")` block). GameState.crafting_bench_item will stay null from `initialize_fresh_game()`.

3. Verify the export/import path: `export_save_string()` calls `_build_save_data()` (auto-updated) and `import_save_string()` calls `_migrate_save()` then `_restore_state()` (both auto-updated). No additional changes needed for import/export.
  </action>
  <verify>
Read the final file and confirm:
- `_build_save_data()` writes arrays for crafting_inventory slots (no single items)
- `_build_save_data()` does NOT include `crafting_bench_item` in return dict
- `_restore_state()` reads arrays from save data, extracts first item for GameState
- `_restore_state()` has no `crafting_bench_item` restoration code
- `crafting_bench_type` is still saved and restored (unchanged)
- The import/export methods don't need changes (they delegate to build/migrate/restore)

Launch the game and verify:
1. Start fresh (delete save if exists) — game starts normally, saving produces v2 format
2. If a v1 save exists, it migrates without error
  </verify>
  <done>
`_build_save_data()` writes per-slot arrays. `_restore_state()` reads arrays and bridges to single-item GameState. `crafting_bench_item` is absent from all save paths. Import/export automatically use the new format.
  </done>
</task>

</tasks>

<verification>
1. **v1 Migration:** A v1 save with `"crafting_inventory": {"weapon": {item_dict}, "helmet": null, ...}` is migrated to `{"weapon": [{item_dict}], "helmet": [], ...}` and `crafting_bench_item` is erased.
2. **v2 Round-trip:** Saving and loading produces identical game state — each slot with an item saves as `[item_dict]` and restores the item; each empty slot saves as `[]` and restores null.
3. **Orphaned Key:** Neither `_build_save_data()` output nor migrated data contains `crafting_bench_item`.
4. **Fresh Game:** A new game save writes version 2 with empty arrays for all 5 crafting slots.
5. **Import/Export:** Export produces v2 format string; importing a v1 string migrates correctly.
</verification>

<success_criteria>
- SAVE_VERSION constant is 2
- `_migrate_v1_to_v2()` function exists and handles all 5 slots
- `_build_save_data()` writes array format for crafting_inventory
- `_restore_state()` reads array format and populates single-item GameState
- `crafting_bench_item` absent from save output and restore logic
- Game starts, saves, and loads without errors
- Existing saves migrate silently on load
</success_criteria>

<output>
After completion, create `.planning/phases/27-save-format-migration/27-01-SUMMARY.md`
</output>
