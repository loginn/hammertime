---
phase: 56-difficulty-starter-kit
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - models/currencies/runic_hammer.gd
  - models/currencies/forge_hammer.gd
  - models/currencies/claw_hammer.gd
  - models/currencies/tuning_hammer.gd
  - autoloads/game_state.gd
autonomous: true
requirements: [DIFF-01, DIFF-03]
gap_closure: true

must_haves:
  truths:
    - "RunicHammer.currency_name is 'Transmute Hammer'"
    - "ForgeHammer.currency_name is 'Augment Hammer'"
    - "ClawHammer.currency_name is 'Chaos Hammer'"
    - "TuningHammer.currency_name is 'Exalt Hammer'"
    - "Forge view tooltip and button text show Transmute, Augment, Chaos, Exalt (not Runic, Forge, Claw, Tuning)"
    - "Fresh game starter items are tier 7: Iron Broadsword, Iron Plate, Iron Dagger, Hide Vest, Bone Wand, Linen Robe"
  artifacts:
    - path: "models/currencies/runic_hammer.gd"
      provides: "Transmute Hammer currency_name and error message"
      contains: "Transmute Hammer"
    - path: "models/currencies/forge_hammer.gd"
      provides: "Augment Hammer currency_name and error message"
      contains: "Augment Hammer"
    - path: "models/currencies/claw_hammer.gd"
      provides: "Chaos Hammer currency_name"
      contains: "Chaos Hammer"
    - path: "models/currencies/tuning_hammer.gd"
      provides: "Exalt Hammer currency_name"
      contains: "Exalt Hammer"
    - path: "autoloads/game_state.gd"
      provides: "Tier 7 starter items in _place_starter_kit()"
      contains: ".new(7)"
  key_links:
    - from: "models/currencies/runic_hammer.gd"
      to: "scenes/forge_view.gd"
      via: "currency_name used in tooltip_text and button.text (lines 323, 340-341)"
      pattern: "currency_name"
---

<objective>
Fix 3 UAT gaps from Phase 56 testing: (1) update currency_name in 4 hammer model files to PoE convention names, (2) update hardcoded error messages referencing old names, (3) change starter item tiers from 8 to 7 so items get correct names (Iron Plate instead of Rusty Plate, etc.).

Purpose: Close UAT test failures 1, 2, and 3 so currency labels display correctly in the Forge view and fresh games start with properly named starter gear.
Output: 5 files updated with correct names and tiers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-difficulty-starter-kit/56-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update currency_name and error messages in 4 hammer model files</name>
  <files>models/currencies/runic_hammer.gd, models/currencies/forge_hammer.gd, models/currencies/claw_hammer.gd, models/currencies/tuning_hammer.gd</files>
  <action>
Update currency_name values in _init() and hardcoded error message strings. TackHammer and GrandHammer already have correct names (Alteration Hammer and Regal Hammer respectively) -- do NOT touch those files.

**runic_hammer.gd:**
- Line 5: change `currency_name = "Runic Hammer"` to `currency_name = "Transmute Hammer"`
- Line 14: change `return "Runic Hammer can only be used on Normal items"` to `return "Transmute Hammer can only be used on Normal items"`

**forge_hammer.gd:**
- Line 5: change `currency_name = "Forge Hammer"` to `currency_name = "Augment Hammer"`
- Line 14: change `return "Forge Hammer can only be used on Normal items"` to `return "Augment Hammer can only be used on Normal items"`

**claw_hammer.gd:**
- Line 5: change `currency_name = "Claw Hammer"` to `currency_name = "Chaos Hammer"`
- (Error message on line 16 says "Item has no mods to remove" -- no name reference, leave as-is)

**tuning_hammer.gd:**
- Line 5: change `currency_name = "Tuning Hammer"` to `currency_name = "Exalt Hammer"`
- (Error message on line 14 says "Item has no mods to reroll" -- no name reference, leave as-is)
  </action>
  <verify>
    <automated>grep -n "currency_name" models/currencies/runic_hammer.gd models/currencies/forge_hammer.gd models/currencies/claw_hammer.gd models/currencies/tuning_hammer.gd | grep -v "Transmute\|Augment\|Chaos\|Exalt" && echo "FAIL: old names remain" || echo "PASS: all names updated"</automated>
  </verify>
  <done>All 6 hammer classes have PoE names: Transmute, Augment, Alteration (already correct), Regal (already correct), Chaos, Exalt. Error messages in runic_hammer.gd and forge_hammer.gd reference new names.</done>
</task>

<task type="auto">
  <name>Task 2: Change starter item tiers from 8 to 7 in _place_starter_kit()</name>
  <files>autoloads/game_state.gd</files>
  <action>
In autoloads/game_state.gd, function `_place_starter_kit()` (starts at line 65), change every `.new(8)` to `.new(7)`. There are 6 occurrences across 3 archetype branches:

- Line 69: `Broadsword.new(8)` to `Broadsword.new(7)`
- Line 70: `IronPlate.new(8)` to `IronPlate.new(7)`
- Line 74: `Broadsword.new(8)` to `Broadsword.new(7)`
- Line 75: `IronPlate.new(8)` to `IronPlate.new(7)`
- Line 77: `Dagger.new(8)` to `Dagger.new(7)`
- Line 78: `LeatherVest.new(8)` to `LeatherVest.new(7)`
- Line 80: `Wand.new(8)` to `Wand.new(7)`
- Line 81: `SilkRobe.new(8)` to `SilkRobe.new(7)`

This gives tier 7 names: Iron Broadsword, Iron Plate, Iron Dagger, Hide Vest, Bone Wand, Linen Robe -- all proper starter-quality names instead of "Rusty/Tattered/Twig/Threadbare" tier 8 names.
  </action>
  <verify>
    <automated>grep -n "\.new(8)" autoloads/game_state.gd | grep -E "Broadsword|IronPlate|Dagger|LeatherVest|Wand|SilkRobe" && echo "FAIL: tier 8 references remain" || echo "PASS: all starter items are tier 7"</automated>
  </verify>
  <done>All starter items in _place_starter_kit() use tier 7. Fresh game produces Iron Broadsword + Iron Plate (STR), Iron Dagger + Hide Vest (DEX), Bone Wand + Linen Robe (INT).</done>
</task>

</tasks>

<verification>
1. `grep "currency_name" models/currencies/*.gd` shows: Transmute, Augment, Alteration, Regal, Chaos, Exalt -- no old names
2. `grep "\.new(" autoloads/game_state.gd | grep -E "sword|Plate|Dagger|Vest|Wand|Robe"` shows all `.new(7)` -- no `.new(8)`
3. No test files reference old currency names (verified during planning)
</verification>

<success_criteria>
- All 6 currency hammer classes use PoE-style currency_name values
- Error messages in runic_hammer.gd and forge_hammer.gd reference new names
- _place_starter_kit() creates tier 7 items for all 3 archetype branches
- UAT tests 1, 2, and 3 are unblocked by these fixes
</success_criteria>

<output>
After completion, create `.planning/phases/56-difficulty-starter-kit/56-04-SUMMARY.md`
</output>
