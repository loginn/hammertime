---
phase: 56-difficulty-starter-kit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - autoloads/game_state.gd
  - autoloads/prestige_manager.gd
  - models/loot/loot_table.gd
  - scenes/forge_view.gd
  - scenes/prestige_view.gd
  - tools/test/integration_test.gd
autonomous: true
requirements: [DIFF-03]
must_haves:
  truths:
    - "All runtime currency dictionary keys use PoE names (transmute, augment, alteration, regal, chaos, exalt)"
    - "Fresh game starts with 2 Transmute and 2 Augment hammers instead of 1 Runic"
    - "Prestige wipe gives 2 Transmute and 2 Augment hammers"
    - "Prestige cost references augment instead of forge"
    - "All existing integration tests pass with renamed keys"
  artifacts:
    - path: "autoloads/game_state.gd"
      provides: "Renamed currency keys in initialize_fresh_game() and _wipe_run_state()"
      contains: '"transmute": 2'
    - path: "autoloads/prestige_manager.gd"
      provides: "Renamed prestige cost keys"
      contains: '"augment": 100'
    - path: "models/loot/loot_table.gd"
      provides: "Renamed drop rate and gate level keys"
      contains: '"transmute"'
    - path: "scenes/forge_view.gd"
      provides: "Renamed HAMMER_INSTANCES, TOOLTIPS, ICONS, BUTTONS keys and bindings"
      contains: '"transmute"'
    - path: "scenes/prestige_view.gd"
      provides: "Renamed label text"
      contains: "Augment"
    - path: "tools/test/integration_test.gd"
      provides: "Updated test assertions with new key names"
      contains: '"transmute"'
  key_links:
    - from: "scenes/forge_view.gd"
      to: "autoloads/game_state.gd"
      via: "currency_counts dictionary key lookup"
      pattern: 'currency_counts\["transmute"\]'
    - from: "autoloads/prestige_manager.gd"
      to: "autoloads/game_state.gd"
      via: "spend_currency uses same key names"
      pattern: '"augment"'
    - from: "models/loot/loot_table.gd"
      to: "autoloads/game_state.gd"
      via: "add_currencies uses same key names"
      pattern: '"transmute"'
---

<objective>
Rename all 6 currency dictionary keys from internal names to PoE conventions and update starter currency counts to 2 Transmute + 2 Augment.

Purpose: Code matches the PoE terminology the user thinks in (per D-05), and fresh games start with the correct starter hammer loadout (per D-04).
Output: All 6 files updated with renamed keys, all existing tests passing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-difficulty-starter-kit/56-CONTEXT.md
@.planning/phases/56-difficulty-starter-kit/56-RESEARCH.md

<interfaces>
<!-- Currency key rename map (D-05): -->
<!-- runic → transmute, forge → augment, tack → alteration -->
<!-- grand → regal, claw → chaos, tuning → exalt -->

<!-- CRITICAL: Do NOT rename "forge" in main_view.gd — those are VIEW names, not currency keys -->
<!-- CRITICAL: Do NOT rename GDScript class names (RunicHammer, ForgeHammer, etc.) — only dict keys -->
<!-- CRITICAL: Do NOT rename asset filenames (runic_hammer.png, etc.) — keep preload paths as-is -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename currency keys across all 6 files and update starter counts</name>
  <files>autoloads/game_state.gd, autoloads/prestige_manager.gd, models/loot/loot_table.gd, scenes/forge_view.gd, scenes/prestige_view.gd, tools/test/integration_test.gd</files>
  <read_first>autoloads/game_state.gd, autoloads/prestige_manager.gd, models/loot/loot_table.gd, scenes/forge_view.gd, scenes/prestige_view.gd, tools/test/integration_test.gd, scenes/main_view.gd</read_first>
  <action>
Apply the following key rename across all 6 files listed. The rename is DICTIONARY KEY ONLY — do not rename GDScript class names (RunicHammer stays RunicHammer), asset filenames (runic_hammer.png stays), or the "forge" view name in main_view.gd.

**Rename map (old → new):**
- `"runic"` → `"transmute"`
- `"forge"` → `"augment"`
- `"tack"` → `"alteration"`
- `"grand"` → `"regal"`
- `"claw"` → `"chaos"`
- `"tuning"` → `"exalt"`

**Per-file changes:**

**autoloads/game_state.gd** (per D-04, D-05):
- `initialize_fresh_game()` lines 100-107: rename all 6 keys AND change starter counts:
  ```
  currency_counts = {
      "transmute": 2,
      "augment": 2,
      "alteration": 0,
      "regal": 0,
      "chaos": 0,
      "exalt": 0
  }
  ```
- `_wipe_run_state()` lines 146-153: same rename AND same starter counts (2 transmute, 2 augment per D-08):
  ```
  currency_counts = {
      "transmute": 2,
      "augment": 2,
      "alteration": 0,
      "regal": 0,
      "chaos": 0,
      "exalt": 0,
  }
  ```
- `debug_hammers` block (~line 74): rename `currency_counts` keys there too if they exist

**autoloads/prestige_manager.gd** (lines 8-14):
- Rename all `"forge"` keys to `"augment"` in `PRESTIGE_COSTS` dict:
  ```
  1: { "augment": 100 },
  2: { "augment": 999999 },
  ...etc for all 7 levels
  ```

**models/loot/loot_table.gd**:
- `GATE_LEVELS` dict (lines 22-27): rename all 6 keys:
  ```
  "transmute": 1,
  "alteration": 1,
  "augment": 15,
  "regal": 40,
  "chaos": 65,
  "exalt": 65,
  ```
- `DROP_RATES` dict (lines 69-74): rename all 6 keys:
  ```
  "transmute": {"chance": 0.25, "max_qty": 2},
  "alteration": {"chance": 0.25, "max_qty": 2},
  "augment": {"chance": 0.25, "max_qty": 1},
  "regal": {"chance": 0.20, "max_qty": 1},
  "chaos": {"chance": 0.20, "max_qty": 1},
  "exalt": {"chance": 0.20, "max_qty": 1},
  ```

**scenes/forge_view.gd**:
- `HAMMER_INSTANCES` dict (lines 41-46): rename keys. Keep values as-is (RunicHammer.new() etc.):
  ```
  "transmute": RunicHammer.new(),
  "augment": ForgeHammer.new(),
  "alteration": TackHammer.new(),
  "regal": GrandHammer.new(),
  "chaos": ClawHammer.new(),
  "exalt": TuningHammer.new(),
  ```
- `TOOLTIPS` dict (lines 77-82): rename keys. Update human-readable names in tooltip text:
  - "Turns a normal item into a magic item..." key becomes `"transmute"` (text can stay or say "Transmute Hammer: ...")
  - "Turns a normal item into a rare item..." key becomes `"augment"`
  - "Adds one random mod to a magic item..." key becomes `"alteration"`
  - "Adds one random mod to a rare item..." key becomes `"regal"`
  - "Removes one random mod..." key becomes `"chaos"`
  - "Rerolls all mod values..." key becomes `"exalt"`
- `ICONS` dict (lines 92-97): rename keys. Keep preload paths pointing to EXISTING asset files:
  ```
  "transmute": preload("res://assets/runic_hammer.png"),
  "augment": preload("res://assets/forge_hammer.png"),
  "alteration": preload("res://assets/tack_hammer.png"),
  "regal": preload("res://assets/grand_hammer.png"),
  "chaos": preload("res://assets/claw_hammer.png"),
  "exalt": preload("res://assets/tuning_hammer.png")
  ```
- `BUTTONS` dict (lines 104-109): rename keys (keep button var names as-is):
  ```
  "transmute": runic_btn,
  "augment": forge_btn,
  "alteration": tack_btn,
  "regal": grand_btn,
  "chaos": claw_btn,
  "exalt": tuning_btn
  ```
- Button `.pressed.connect()` calls (lines 118-123): update bind strings:
  ```
  runic_btn.pressed.connect(_on_currency_selected.bind("transmute"))
  forge_btn.pressed.connect(_on_currency_selected.bind("augment"))
  tack_btn.pressed.connect(_on_currency_selected.bind("alteration"))
  grand_btn.pressed.connect(_on_currency_selected.bind("regal"))
  claw_btn.pressed.connect(_on_currency_selected.bind("chaos"))
  tuning_btn.pressed.connect(_on_currency_selected.bind("exalt"))
  ```
- `standard_types` array (line 313): rename all 6 entries:
  ```
  var standard_types: Array = ["transmute", "augment", "alteration", "regal", "chaos", "exalt"]
  ```

**scenes/prestige_view.gd** (lines 39, 111):
- Line 39: change `"Next: " + str(cost["forge"]) + " Forge Hammers"` to `"Next: " + str(cost["augment"]) + " Augment Hammers"`
- Line 111: change `str(PrestigeManager.PRESTIGE_COSTS[level]["forge"]) + " Forge"` to `str(PrestigeManager.PRESTIGE_COSTS[level]["augment"]) + " Augment"`

**tools/test/integration_test.gd**:
- All occurrences of `currency_counts["forge"]` → `currency_counts["augment"]`
- All occurrences of `currency_counts["runic"]` → `currency_counts["transmute"]`
- `spend_currency("forge")` → `spend_currency("augment")`
- Update assertion messages to match new key names (e.g., "forge currency == 0" → "augment currency == 0")
- Update expected default value: old default was `"runic": 1`, new default is `"transmute": 2`
- Where tests set `currency_counts["forge"] = 100` for prestige, change to `currency_counts["augment"] = 100`
- Where tests check `currency_counts["runic"] == 1` for fresh default, change to `currency_counts["transmute"] == 2`
  </action>
  <verify>
    <automated>grep -n '"runic"\|"forge"\|"tack"\|"grand"\|"claw"\|"tuning"' autoloads/game_state.gd autoloads/prestige_manager.gd models/loot/loot_table.gd scenes/forge_view.gd scenes/prestige_view.gd tools/test/integration_test.gd | grep -v "preload\|_hammer\\.png\|# " | head -5; echo "Expected: 0 matches (all renamed)"</automated>
  </verify>
  <acceptance_criteria>
    - autoloads/game_state.gd contains `"transmute": 2` in both initialize_fresh_game() and _wipe_run_state()
    - autoloads/game_state.gd contains `"augment": 2` in both initialize_fresh_game() and _wipe_run_state()
    - autoloads/game_state.gd does NOT contain `"runic"` or `"forge"` as currency keys (grep excluding comments)
    - autoloads/prestige_manager.gd contains `"augment": 100` and no `"forge"` keys
    - models/loot/loot_table.gd contains `"transmute"` and `"augment"` keys, no `"runic"` or `"forge"` keys
    - scenes/forge_view.gd contains `"transmute": RunicHammer.new()` and `"augment": ForgeHammer.new()`
    - scenes/forge_view.gd preload paths still reference `res://assets/runic_hammer.png` etc. (unchanged asset paths)
    - scenes/prestige_view.gd contains `"Augment Hammers"` and `"Augment"` labels, no `"Forge Hammers"` or `"Forge"` labels
    - tools/test/integration_test.gd contains `currency_counts["transmute"]` and `currency_counts["augment"]`, no old keys
    - scenes/main_view.gd is UNTOUCHED (the "forge" strings there are view names, not currency keys)
    - No GDScript class names were renamed (RunicHammer, ForgeHammer, etc. still exist)
  </acceptance_criteria>
  <done>All 6 currency dictionary keys renamed across all 6 files. Starter counts updated to 2 transmute + 2 augment. Asset paths and class names unchanged. No old key strings remain in affected files (excluding comments and asset paths).</done>
</task>

</tasks>

<verification>
- grep for old key names ("runic", "forge", "tack", "grand", "claw", "tuning") across all 6 files — should return 0 matches (excluding asset preload paths and comments)
- grep for new key names ("transmute", "augment", "alteration", "regal", "chaos", "exalt") across all 6 files — should return matches in expected locations
- scenes/main_view.gd must still contain `show_view("forge")` and `current_view == "forge"` (view names unchanged)
</verification>

<success_criteria>
- All 6 currency keys renamed in all 6 files per D-05 rename map
- Starter currency counts are 2 transmute + 2 augment in both initialize_fresh_game() and _wipe_run_state() per D-04/D-08
- Asset preload paths unchanged (still point to runic_hammer.png etc.)
- GDScript class names unchanged (RunicHammer, ForgeHammer etc.)
- main_view.gd "forge" view name strings unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/56-difficulty-starter-kit/56-01-SUMMARY.md`
</output>
