---
phase: 23-damage-range-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - models/items/weapon.gd
  - models/items/light_sword.gd
  - models/monsters/pack_generator.gd
autonomous: true
requirements:
  - DMG-01
  - DMG-03

must_haves:
  truths:
    - "Weapon has base_damage_min and base_damage_max fields; base_damage returns their average"
    - "LightSword sets Physical-ratio range values (tight 1:1.5 spread)"
    - "Element variance constants exist for all four elements with correct ratios"
    - "Physical has tightest spread (1:1.5) and Lightning has widest (1:4)"
  artifacts:
    - path: "models/items/weapon.gd"
      provides: "base_damage_min, base_damage_max fields and computed base_damage property"
      contains: "base_damage_min"
    - path: "models/items/light_sword.gd"
      provides: "Weapon subclass using range values instead of single base_damage"
      contains: "base_damage_min"
    - path: "models/monsters/pack_generator.gd"
      provides: "ELEMENT_VARIANCE dictionary with min_mult/max_mult per element"
      contains: "ELEMENT_VARIANCE"
  key_links:
    - from: "models/items/weapon.gd"
      to: "StatCalculator.calculate_dps"
      via: "computed base_damage property returns (min+max)/2"
      pattern: "base_damage.*get.*base_damage_min.*base_damage_max"
    - from: "models/items/light_sword.gd"
      to: "models/items/weapon.gd"
      via: "sets base_damage_min and base_damage_max instead of base_damage"
      pattern: "base_damage_min.*=.*\\d"
---

<objective>
Add weapon base damage range fields and element variance constants as the foundation for the damage range data model.

Purpose: Weapons need min-max damage ranges instead of single values, and element variance ratios define how wide each element's spread is. These are the foundational data structures that Plan 02 (affixes + monster packs) builds upon.
Output: Weapon.gd with range fields and computed backward-compat property, LightSword using ranges, ELEMENT_VARIANCE constants in PackGenerator.
</objective>

<execution_context>
@/home/travelboi/.claude/get-shit-done/workflows/execute-plan.md
@/home/travelboi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-damage-range-data-model/23-RESEARCH.md
@models/items/weapon.gd
@models/items/light_sword.gd
@models/monsters/pack_generator.gd
@utils/stat_calculator.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add weapon base damage range fields with backward-compatible computed property</name>
  <files>models/items/weapon.gd, models/items/light_sword.gd</files>
  <action>
In `models/items/weapon.gd`:
- Replace `var base_damage: int` with two new fields:
  ```
  var base_damage_min: int = 0
  var base_damage_max: int = 0
  ```
- Add a computed property for backward compatibility that returns the integer average:
  ```
  var base_damage: int:
      get: return (base_damage_min + base_damage_max) / 2
  ```
  This ensures `StatCalculator.calculate_dps(self.base_damage, ...)` in `update_value()` continues to work without changes. The `calculate_dps` signature is NOT changed in this phase (that is Phase 24).
- All other fields (base_damage_type, base_speed, dps, etc.) remain unchanged.
- The `update_value()` method body remains unchanged -- it already calls `StatCalculator.calculate_dps(self.base_damage, ...)` which will now read the computed average.

In `models/items/light_sword.gd`:
- Replace `self.base_damage = 10` with range values using Physical element's tight 1:1.5 variance ratio:
  ```
  self.base_damage_min = 8
  self.base_damage_max = 12
  ```
  Math: avg = (8+12)/2 = 10 (same as before). Ratio 8:12 = 1:1.5 (Physical tight spread). DPS is unchanged because `base_damage` computed property returns 10.
- All other fields in `_init()` remain unchanged.

Do NOT touch `StatCalculator`, `CombatEngine`, or any UI files. This is data model only.
  </action>
  <verify>
Open the Godot project and run the scene. The game should launch without errors. LightSword DPS should display the same value as before (base_damage computed property returns 10, same as the old hard-coded value). Verify with: `print(LightSword.new().base_damage)` in a test script outputs 10. Alternatively, grep weapon.gd for `base_damage_min` and `base_damage_max` fields and the computed `base_damage` property.
  </verify>
  <done>
weapon.gd has base_damage_min and base_damage_max fields with a computed base_damage property returning their integer average. LightSword sets base_damage_min=8, base_damage_max=12 (avg=10, ratio 1:1.5). Game runs without errors; DPS display is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define element variance constants in PackGenerator</name>
  <files>models/monsters/pack_generator.gd</files>
  <action>
In `models/monsters/pack_generator.gd`, add a class-level constant dictionary after the existing constants (PACK_COUNT_MIN, PACK_COUNT_MAX, GROWTH_RATE):

```gdscript
## Element variance: multipliers applied to scaled base damage to produce min/max range.
## Ratios from project decisions: Physical 1:1.5, Cold 1:2, Fire 1:2.5, Lightning 1:4.
## Formula: for ratio lo:hi, min_mult = 2*lo/(lo+hi), max_mult = 2*hi/(lo+hi).
## Average is always preserved: (min + max) / 2 = base.
const ELEMENT_VARIANCE: Dictionary = {
    "physical": {"min_mult": 0.80, "max_mult": 1.20},
    "cold": {"min_mult": 0.667, "max_mult": 1.333},
    "fire": {"min_mult": 0.571, "max_mult": 1.429},
    "lightning": {"min_mult": 0.40, "max_mult": 1.60},
}
```

Math verification (per locked decisions in STATE.md):
- Physical at base=10: min=8.0, max=12.0, ratio 8:12 = 1:1.5. Correct.
- Cold at base=10: min=6.67, max=13.33, ratio 6.67:13.33 = 1:2. Correct.
- Fire at base=10: min=5.71, max=14.29, ratio 5.71:14.29 = 1:2.5. Correct.
- Lightning at base=10: min=4.0, max=16.0, ratio 4:16 = 1:4. Correct.

Do NOT modify `create_pack()` or any other method in this task. The constants are added now; `create_pack()` is updated in Plan 02 Task 2 to use them.

Place the constant in PackGenerator (Option A from research) rather than a separate file. If Phase 25 needs them elsewhere, they can reference `PackGenerator.ELEMENT_VARIANCE` or refactor then.
  </action>
  <verify>
Grep pack_generator.gd for "ELEMENT_VARIANCE" to confirm the constant exists. Verify the dictionary has exactly 4 keys: "physical", "cold", "fire", "lightning". Verify each entry has "min_mult" and "max_mult" keys. Verify `min_mult + max_mult = 2.0` for each element (average preservation). The game should run without errors since no behavior changed.
  </verify>
  <done>
ELEMENT_VARIANCE constant exists in PackGenerator with correct multipliers for all four elements. Physical has tightest spread (0.80-1.20), Lightning has widest (0.40-1.60). Average is preserved for all elements. No runtime behavior changed.
  </done>
</task>

</tasks>

<verification>
1. `weapon.gd` has `base_damage_min`, `base_damage_max` fields and computed `base_damage` property
2. `light_sword.gd` sets `base_damage_min = 8` and `base_damage_max = 12`
3. `pack_generator.gd` has `ELEMENT_VARIANCE` constant with 4 element entries
4. Game launches without errors
5. DPS display is unchanged (backward compatibility via computed property)
</verification>

<success_criteria>
- Weapon expresses damage as a min-max range with backward-compatible average property
- Element variance constants define the locked ratios (Physical 1:1.5 through Lightning 1:4)
- Zero behavioral changes -- all existing game functionality identical
</success_criteria>

<output>
After completion, create `.planning/phases/23-damage-range-data-model/23-01-SUMMARY.md`
</output>
