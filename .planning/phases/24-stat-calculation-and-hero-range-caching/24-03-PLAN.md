---
phase: 24-stat-calculation-and-hero-range-caching
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [scenes/forge_view.gd]
autonomous: true
requirements: [STAT-01]
gap_closure: true

must_haves:
  truths:
    - "Armor/Helmet/Boots comparison uses defensive stat scoring, not tier"
    - "Defensive score sums base stats (armor, evasion, ES, health) plus type-specific stats (mana for helmet, movement speed for boots) plus all resistances from suffixes"
    - "A tier-3 item with better defensive stats beats a tier-5 item with worse stats"
  artifacts:
    - path: "scenes/forge_view.gd"
      provides: "_defensive_score() helper and updated is_item_better()"
      contains: "_defensive_score"
  key_links:
    - from: "scenes/forge_view.gd::is_item_better()"
      to: "scenes/forge_view.gd::_defensive_score()"
      via: "direct call for non-weapon/ring items"
      pattern: "_defensive_score\\(new_item\\).*_defensive_score\\(existing_item\\)"
    - from: "scenes/forge_view.gd::_defensive_score()"
      to: "scenes/forge_view.gd::_sum_suffix_stat()"
      via: "resistance calculation reuse"
      pattern: "_sum_suffix_stat\\(item.*RESISTANCE"
---

<objective>
Replace tier-based comparison for armor/helmet/boots with a defensive stat scoring function.

Purpose: UAT test 4 revealed that tier comparison is not meaningful for defensive items. The user wants comparison based on actual defensive stats (armor, evasion, ES, HP, resistances). All data already exists on items -- just need to sum it.

Output: Updated `is_item_better()` that uses `_defensive_score()` for non-weapon/ring items.
</objective>

<execution_context>
@/home/travelboi/.claude/get-shit-done/workflows/execute-plan.md
@/home/travelboi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scenes/forge_view.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _defensive_score() helper and update is_item_better()</name>
  <files>scenes/forge_view.gd</files>
  <action>
Add a new `_defensive_score(item: Item) -> int` helper function in `scenes/forge_view.gd`, placed between `is_item_better()` and the display section (before line 475's comment block).

The function sums up ALL defensive value on an item into a single score:

1. Base stats (read directly from item properties -- these exist on Armor, Helmet, and Boots):
   - `item.base_armor` (if property exists on the item)
   - `item.base_evasion` (if property exists)
   - `item.base_energy_shield` (if property exists)
   - `item.base_health` (if property exists)
   - `item.base_mana` (if property exists -- Helmet only)
   - `item.base_movement_speed` (if property exists -- Boots only)

   Use `item.get("property_name")` or check with `"property_name" in item` to safely read properties that may not exist on all types. Since GDScript Resources support `get()`, use: `var val = item.get("base_armor")` which returns null if not present, then add `val if val else 0`.

2. Resistances from suffixes (using the existing `_sum_suffix_stat()` helper already in forge_view.gd):
   - `_sum_suffix_stat(item, Tag.StatType.FIRE_RESISTANCE)`
   - `_sum_suffix_stat(item, Tag.StatType.COLD_RESISTANCE)`
   - `_sum_suffix_stat(item, Tag.StatType.LIGHTNING_RESISTANCE)`
   - `_sum_suffix_stat(item, Tag.StatType.ALL_RESISTANCE) * 3` (all-res contributes to all three elements)

Sum everything into a single int and return it.

Then update `is_item_better()` (currently at line 465-472). Replace the last line:
```
return new_item.tier > existing_item.tier
```
with:
```
return _defensive_score(new_item) > _defensive_score(existing_item)
```

Keep the Weapon DPS and Ring DPS comparisons unchanged (lines 467-470).

Do NOT change any other function in the file. This is a surgical two-part edit: add the helper, update one line in is_item_better().
  </action>
  <verify>
Verify by searching forge_view.gd for:
1. `func _defensive_score` exists
2. `_defensive_score(new_item) > _defensive_score(existing_item)` exists in is_item_better()
3. The old `new_item.tier > existing_item.tier` line is gone
4. `_sum_suffix_stat` is called within _defensive_score for resistance stats
5. No syntax errors: run `grep -c "func " scenes/forge_view.gd` to confirm function count increased by 1
  </verify>
  <done>
is_item_better() compares armor/helmet/boots by defensive score (sum of base_armor + base_evasion + base_energy_shield + base_health + base_mana + base_movement_speed + fire_res + cold_res + lightning_res + all_res*3) instead of tier. Weapon and Ring comparison unchanged (still DPS-based).
  </done>
</task>

</tasks>

<verification>
- `_defensive_score()` helper function exists in forge_view.gd
- `is_item_better()` uses `_defensive_score()` for non-weapon/ring items
- No reference to `tier` remains in `is_item_better()`
- `_sum_suffix_stat()` is called for resistance values within `_defensive_score()`
- Weapon DPS comparison unchanged
- Ring DPS comparison unchanged
</verification>

<success_criteria>
When a new armor/helmet/boots drop arrives, `is_item_better()` compares it against the existing item using a sum of all defensive stats (armor, evasion, ES, health, mana, movement speed, and resistances from suffixes) rather than raw item tier. An item with better total defensive stats replaces the existing item regardless of tier difference.
</success_criteria>

<output>
After completion, create `.planning/phases/24-stat-calculation-and-hero-range-caching/24-03-SUMMARY.md`
</output>
