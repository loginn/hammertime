---
phase: 49
plan: 01
title: "Save Version Bump & Integration Verification"
wave: 1
depends_on: []
files_modified:
  - autoloads/save_manager.gd
  - tools/test/integration_test.gd
requirements:
  - LOOT-01
  - LOOT-02
  - LOOT-03
  - LOOT-04
autonomous: true
---

# Plan 49-01: Save Version Bump & Integration Verification

## Overview

Final integration pass for v1.8. Bump save version from 6 to 7 to invalidate old saves, verify the drop pool contains all 21 item bases with correct distribution, and close out LOOT-03/LOOT-04 as dropped by user decision. Add integration tests covering all of the above.

## Tasks

<task id="1">
<action>Bump SAVE_VERSION from 6 to 7 in `autoloads/save_manager.gd`</action>
<context>
File: `autoloads/save_manager.gd`, line 4.
Current value: `const SAVE_VERSION = 6`
Change to: `const SAVE_VERSION = 7`

No save format changes are needed — the v6 structure already handles all 21 item types, spell mode, and DoT stats. The version bump ensures old saves from before v1.8 are wiped on load. The existing wipe-on-old-version logic at lines 61-65 (`if saved_version < SAVE_VERSION`) handles this automatically by calling `delete_save()` and returning false, which triggers a fresh game.
</context>
<verify>
- `const SAVE_VERSION = 7` appears at line 4
- The `load_game()` method at line 62 compares `saved_version < SAVE_VERSION`, so any save with version < 7 will be deleted
- No other code changes needed in save_manager.gd
</verify>
</task>

<task id="2">
<action>Verify all 21 item bases are present in the drop pool with slot-first-then-archetype distribution</action>
<context>
File: `scenes/gameplay_view.gd`, lines 404-424 (`get_random_item_base()`).

The drop pool was shipped in Phase 44. Verify the following bases are present:
- **Weapon (9):** Broadsword, Battleaxe, Warhammer (STR), Dagger, VenomBlade, Shortbow (DEX), Wand, LightningRod, Sceptre (INT)
- **Armor (3):** IronPlate (STR), LeatherVest (DEX), SilkRobe (INT)
- **Helmet (3):** IronHelm (STR), LeatherHood (DEX), Circlet (INT)
- **Boots (3):** IronGreaves (STR), LeatherBoots (DEX), SilkSlippers (INT)
- **Ring (3):** IronBand (STR), JadeRing (DEX), SapphireRing (INT)

Distribution logic: slot is chosen first (`randi() % 5` = 20% each), then a random base within that slot is chosen (weapon: 1/9 each; other slots: 1/3 each = 33% per archetype within slot).

All 21 bases are already present. No code changes expected — verification only.
</context>
<verify>
- Count exactly 21 unique item base classes across all 5 slot arrays
- Weapon array has exactly 9 entries (3 per archetype)
- Armor, Helmet, Boots, Ring arrays each have exactly 3 entries (1 per archetype)
- Slot selection uses `randi() % slots.size()` (uniform 20%)
- Base selection within slot uses `randi() % slot_bases.size()` (uniform within slot)
</verify>
</task>

<task id="3">
<action>Close LOOT-03 and LOOT-04 as dropped by user decision</action>
<context>
Per user decisions in `49-CONTEXT.md`:

**LOOT-03 (Combined DPS comparison):** Dropped. Tier-only comparison stays in `forge_view.gd:is_item_better()` (line 606-607: `return new_item.tier > existing_item.tier`). Players judge DPS themselves from the stat panel. No code changes.

**LOOT-04 (Archetype labels):** Dropped. Item names are self-documenting (Dagger = DEX, Wand = INT, Broadsword = STR). Archetype identity exists in code via `valid_tags` but does not need UI exposure. No code changes.

Both requirements are satisfied by the explicit design decision to not implement them.
</context>
<verify>
- `forge_view.gd:is_item_better()` still uses tier-only comparison (no DPS logic added)
- No archetype label code was added to any UI file
- Both decisions are documented in `49-CONTEXT.md`
</verify>
</task>

<task id="4">
<action>Add Group 35 integration tests for Phase 49 verification</action>
<context>
File: `tools/test/integration_test.gd`

Add `_group_35_save_version_and_loot_integration()` to the test suite. This group should verify:

1. **SAVE_VERSION is 7:** Read `SaveManager.SAVE_VERSION` and assert it equals 7
2. **Drop pool completeness:** Call `get_random_item_base()` in a loop (e.g., 500 iterations) and collect results, verifying:
   - Items from all 5 slots appear (weapon, armor, helmet, boots, ring)
   - Items from all 3 archetypes appear (check for STR/DEX/INT via valid_tags containing Tag.STR, Tag.DEX, Tag.INT)
   - All 21 unique item type strings appear in the collected set
3. **Old save wipe logic:** Verify `SaveManager.SAVE_VERSION > 6` (ensures old saves trigger the wipe path)
4. **is_item_better uses tier only:** Verify `is_item_better()` returns true when new tier > old tier, false otherwise

Wire the new group into `_ready()` after `_group_34_game_events_dot_signals()`.
</context>
<verify>
- New group function exists and is called from `_ready()`
- Tests cover SAVE_VERSION value, drop pool coverage, old save wipe, and tier comparison
- All tests pass when run in Godot editor (F6)
</verify>
</task>

## Requirements Closure

| Requirement | Status | Resolution |
|-------------|--------|------------|
| LOOT-01 | Complete | All 21 bases verified in drop pool (shipped in Phase 44) |
| LOOT-02 | Complete | SAVE_VERSION bumped from 6 to 7 (Task 1) |
| LOOT-03 | Closed — dropped | User decision: tier-only comparison stays (49-CONTEXT.md) |
| LOOT-04 | Closed — dropped | User decision: item names are self-documenting (49-CONTEXT.md) |

## must_haves
- [ ] SAVE_VERSION is 7 in save_manager.gd
- [ ] Old saves (< v7) trigger fresh game on load
- [ ] All 21 item bases present in drop pool
- [ ] Drop distribution is slot-first (20% each) then archetype (33% each)
- [ ] LOOT-03 and LOOT-04 documented as dropped per user decision
