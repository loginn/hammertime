---
phase: 37-affix-tier-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - autoloads/item_affixes.gd
  - autoloads/save_manager.gd
autonomous: true
requirements:
  - AFFIX-01
  - AFFIX-02

must_haves:
  truths:
    - "Every active affix definition in item_affixes.gd has tier_range set to Vector2i(1, 32) — no affix uses the old 8-tier or 30-tier default"
    - "Resistance affixes (Fire, Cold, Lightning, All) have base_min=1, base_max=2 so tier-1 ceiling is 32-64% (not the game-breaking 160-384% from old bases)"
    - "Flat damage affixes preserve their element-specific spread ratios: Physical 1:1.5, Lightning 1:4, Fire 1:2.5, Cold 1:2"
    - "Tier-32 floor values for all affixes are within 10% of the old worst-tier values (pre-prestige baseline preserved)"
    - "SAVE_VERSION is 4 and loading a v3 save file triggers delete-and-fresh-start"
    - "No affix.quality() function exists — is_item_better() is unchanged"
  artifacts:
    - path: "autoloads/item_affixes.gd"
      provides: "All 27 active affix definitions with Vector2i(1, 32) tier ranges and retuned base values"
      contains: "Vector2i(1, 32)"
    - path: "autoloads/save_manager.gd"
      provides: "Save version 4 with existing delete-on-old-version policy"
      contains: "SAVE_VERSION = 4"
  key_links:
    - from: "autoloads/item_affixes.gd"
      to: "models/affixes/affix.gd"
      via: "Affix.new() constructor uses tier_range in scaling formula"
      pattern: "tier_range.y + 1 - tier"
    - from: "autoloads/save_manager.gd"
      to: "load_game()"
      via: "version check deletes old saves"
      pattern: "saved_version < SAVE_VERSION"
---

<objective>
Expand all affix tier ranges from mixed ranges (5/8/30) to a uniform 32-tier range, retune base values for balanced scaling, and bump the save version to 4.

Purpose: 32 uniform affix tiers create the granular upgrade path that the prestige system needs — P0 characters roll from the worst tiers while P7 characters access tier 1. This phase lays the affix data foundation that Phase 38 (Item Tier System) will gate by item_tier.
Output: Updated autoloads/item_affixes.gd with all affixes at Vector2i(1, 32) and retuned bases; updated autoloads/save_manager.gd with SAVE_VERSION = 4.

Note on AFFIX-02: User decided NOT to implement an affix.quality() function. is_item_better() will use item_tier (Phase 38), not individual affix quality. AFFIX-02 is satisfied by the tier_range expansion to a uniform 32-tier scale, which makes normalized comparison possible when Phase 38 adds item_tier gating. No code change is needed for AFFIX-02 beyond the tier_range uniformization done in AFFIX-01.
</objective>

<execution_context>
@C:/Users/vince/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vince/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-affix-tier-expansion/37-RESEARCH.md
@autoloads/item_affixes.gd
@models/affixes/affix.gd
@autoloads/save_manager.gd
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From models/affixes/affix.gd — Constructor signature (DO NOT CHANGE):
```gdscript
func _init(
    p_name: String = "",
    p_type: AffixType = AffixType.PREFIX,
    p_min: int = 0,          # base_min — tier-32 floor for scalar affixes
    p_max: int = 0,          # base_max — tier-32 ceiling for scalar affixes
    p_tags: Array[String] = [],
    p_stat_types: Array[int] = [],
    p_tier_range: Vector2i = Vector2i(1, 8),  # changing data, not this default
    p_dmg_min_lo: int = 0,   # tier-32 floor for flat damage min-lo
    p_dmg_min_hi: int = 0,   # tier-32 floor for flat damage min-hi
    p_dmg_max_lo: int = 0,   # tier-32 floor for flat damage max-lo
    p_dmg_max_hi: int = 0    # tier-32 floor for flat damage max-hi
) -> void:
```

Tier scaling formula (DO NOT CHANGE):
```gdscript
self.min_value = p_min * (tier_range.y + 1 - tier)
self.max_value = p_max * (tier_range.y + 1 - tier)
# At Vector2i(1, 32): tier 1 multiplier = 32, tier 32 multiplier = 1
# base_min/base_max ARE the tier-32 floor values
```

From autoloads/save_manager.gd — Version constant:
```gdscript
const SAVE_VERSION = 3  # → change to 4
# Existing delete-on-old-version logic handles the rest automatically
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Expand all affix tier ranges to 32 and retune base values</name>
  <files>autoloads/item_affixes.gd</files>
  <action>
In autoloads/item_affixes.gd, update every Affix.new() call to use `Vector2i(1, 32)` tier ranges with retuned base values. The file has 18 active prefixes and 9 active suffixes (27 total). Disabled/commented suffixes stay commented — do not touch them.

CRITICAL REMINDERS:
- `base_min` and `base_max` are the TIER-32 FLOOR values (multiplied by 1 at worst tier)
- Tier-1 ceiling = base * 32
- The scaling formula is LOCKED — do NOT change models/affixes/affix.gd
- Preserve element-specific damage spread ratios per user decision

**PREFIXES — Flat Weapon Damage (4-bound system):**

These affixes use the dmg_min_lo/hi and dmg_max_lo/hi parameters. The base_min/base_max scalar params are unused for FLAT_DAMAGE stat types but must be present for the constructor signature. Keep them as small placeholders (2, 10).

1. `Physical Damage` — tight 1:1.5 ratio. Change `Vector2i(1, 8)` to `Vector2i(1, 32)`. Keep damage bounds: `3, 5, 7, 10`. These ARE the tier-32 floor values; tier-1 = 96, 160, 224, 320.

2. `Lightning Damage` — extreme 1:4 ratio (widest spread). Change `Vector2i(1, 8)` to `Vector2i(1, 32)`. Keep damage bounds: `1, 3, 8, 16`. Tier-1 = 32, 96, 256, 512.

3. `Fire Damage` — wide 1:2.5 ratio. Change `Vector2i(1, 8)` to `Vector2i(1, 32)`. Keep damage bounds: `2, 4, 8, 14`. Tier-1 = 64, 128, 256, 448.

4. `Cold Damage` — moderate 1:2 ratio. Change `Vector2i(1, 8)` to `Vector2i(1, 32)`. Keep damage bounds: `2, 5, 7, 12`. Tier-1 = 64, 160, 224, 384.

**PREFIXES — Percentage Damage (scalar, INCREASED_DAMAGE):**

These 5 affixes currently OMIT the tier_range parameter (defaulting to 8). Add explicit `Vector2i(1, 32)` as the 7th constructor argument. Keep base_min=2, base_max=10. Tier-32 floor = 2-10%, tier-1 = 64-320%. Large percentages are fine per locked decision.

5. `%Physical Damage` — Add `Vector2i(1, 32)` after the stat_types array. Bases stay 2, 10.
6. `%Elemental Damage` — Add `Vector2i(1, 32)` after the stat_types array. Bases stay 2, 10.
7. `%Cold Damage` — Add `Vector2i(1, 32)` after the stat_types array. Bases stay 2, 10.
8. `%Fire Damage` — Add `Vector2i(1, 32)` after the stat_types array. Bases stay 2, 10.
9. `%Lightning Damage` — Add `Vector2i(1, 32)` after the stat_types array. Bases stay 2, 10.

**PREFIXES — Defensive (currently 30-tier):**

Change `Vector2i(1, 30)` to `Vector2i(1, 32)` for all. Base values stay the same — the 30-tier to 32-tier change is minimal (old tier-30 floor = base*1, new tier-32 floor = base*1, same floor). Tier-1 ceiling goes from 30x to 32x.

10. `Flat Armor` — bases 2, 5. Change to `Vector2i(1, 32)`.
11. `%Armor` — bases 1, 3. Change to `Vector2i(1, 32)`.
12. `Evasion` — bases 2, 5. Change to `Vector2i(1, 32)`.
13. `%Evasion` — bases 1, 3. Change to `Vector2i(1, 32)`.
14. `Energy Shield` — bases 3, 6. Change to `Vector2i(1, 32)`.
15. `%Energy Shield` — bases 1, 3. Change to `Vector2i(1, 32)`.
16. `Health` — bases 3, 8. Change to `Vector2i(1, 32)`.
17. `%Health` — bases 1, 3. Change to `Vector2i(1, 32)`.
18. `Mana` — bases 2, 6. Change to `Vector2i(1, 32)`.

**SUFFIXES — Scalar (currently omit tier_range, defaulting to 8):**

Add explicit `Vector2i(1, 32)` as the 7th constructor argument. Keep base values.

1. `Attack Speed` — bases 2, 10. Add `Vector2i(1, 32)`.
2. `Life` — bases 2, 10. Add `Vector2i(1, 32)`. Note: This suffix overlaps with the Health prefix (both FLAT_HEALTH). The suffix has higher base_max (10 vs 8) — this creates intentional variety where suffixes can roll higher single-tier values but both scale to 32 tiers. Leave as-is.
3. `Armor` — bases 2, 10. Add `Vector2i(1, 32)`. Similar overlap with Flat Armor prefix — suffix is the stronger-per-tier variant.

**SUFFIXES — Resistance (REQUIRES RETUNING):**

These need significant base value reduction. Old bases at 32 tiers would produce game-breaking resistance percentages at tier 1.

4. `Fire Resistance` — Change bases from `5, 12` to `1, 2`. Change `Vector2i(1, 8)` to `Vector2i(1, 32)`. Tier-32 floor = 1-2%, tier-1 = 32-64%.
5. `Cold Resistance` — Change bases from `5, 12` to `1, 2`. Change `Vector2i(1, 8)` to `Vector2i(1, 32)`. Tier-32 floor = 1-2%, tier-1 = 32-64%.
6. `Lightning Resistance` — Change bases from `5, 12` to `1, 2`. Change `Vector2i(1, 8)` to `Vector2i(1, 32)`. Tier-32 floor = 1-2%, tier-1 = 32-64%.
7. `All Resistances` — Change bases from `3, 8` to `1, 2`. Change `Vector2i(1, 5)` to `Vector2i(1, 32)`. Tier-32 floor = 1-2%, tier-1 = 32-64%.

**SUFFIXES — Crit (currently omit tier_range, defaulting to 8):**

8. `Critical Strike Chance` — bases 2, 10. Add `Vector2i(1, 32)`.
9. `Critical Strike Damage` — bases 2, 10. Add `Vector2i(1, 32)`.

**VERIFICATION CHECKLIST after editing:**
- Count Affix.new() calls: should be exactly 18 prefixes + 9 suffixes = 27 active
- Every single one has `Vector2i(1, 32)` — grep for `Vector2i(1,` and confirm only `Vector2i(1, 32)` appears (no 5, 8, or 30)
- Resistance affixes have bases `1, 2` (not old `5, 12` or `3, 8`)
- Disabled/commented suffixes are untouched
- No changes to from_affix() static method
  </action>
  <verify>
Grep item_affixes.gd to confirm:
1. `Vector2i(1, 32)` appears exactly 27 times (once per active affix)
2. `Vector2i(1, 8)` appears 0 times in uncommented code
3. `Vector2i(1, 30)` appears 0 times
4. `Vector2i(1, 5)` appears 0 times
5. Resistance base values: grep for "Resistance" lines — confirm bases are `1, 2`
6. No changes to from_affix() method
  </verify>
  <done>
All 27 active affix definitions have tier_range = Vector2i(1, 32). Flat damage affixes preserve their element-specific spread ratios. Percentage and scalar affixes keep their existing base values. Resistance affixes are retuned to base_min=1, base_max=2 (tier-1 ceiling = 32-64%). The 10 affixes that previously used implicit 8-tier defaults now have explicit Vector2i(1, 32). Disabled suffixes are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump save version to 4</name>
  <files>autoloads/save_manager.gd</files>
  <action>
In autoloads/save_manager.gd, change the SAVE_VERSION constant:

Change `const SAVE_VERSION = 3` to `const SAVE_VERSION = 4`.

That is the ONLY change. The existing delete-on-old-version logic in load_game() already handles the rest:
```gdscript
if saved_version < SAVE_VERSION:
    push_warning("SaveManager: Outdated save (v%d), deleting and starting fresh" % saved_version)
    delete_save()
    return false
```

This will automatically delete v3 saves and start fresh, which is required because existing v3 saves contain items with old tier ranges (8-tier and 30-tier affixes) that would be inconsistent with the new 32-tier system.

The import_save_string() path also works correctly — the `> SAVE_VERSION` check rejects saves from newer versions, and old-version imports get restored with _restore_state() defaults. Items from old imports will have stale tier ranges in their serialized affix data, but this is acceptable: the game starts fresh from the file save path, and import strings from v3 will produce items with old tier data that still function (the Affix.from_dict() restores whatever was serialized). Per user decision, there are no live players — backward compatibility is not a concern.

DO NOT change anything else in save_manager.gd. No new fields, no migration logic, no structural changes.
  </action>
  <verify>
Grep save_manager.gd for:
1. `SAVE_VERSION = 4` exists
2. `SAVE_VERSION = 3` does NOT exist
3. The delete-on-old-version block is unchanged (still references `saved_version < SAVE_VERSION`)
  </verify>
  <done>
SAVE_VERSION is 4. Old v3 saves are automatically deleted on load via the existing delete-on-old-version policy. No other changes to save_manager.gd.
  </done>
</task>

</tasks>

<verification>
1. Verify item_affixes.gd has no GDScript syntax errors (all Affix.new() calls have correct argument counts)
2. Confirm all 27 active affixes have Vector2i(1, 32) — no instances of Vector2i(1, 8), Vector2i(1, 30), or Vector2i(1, 5) in uncommented code
3. Confirm resistance affixes use base_min=1, base_max=2
4. Confirm flat damage affixes preserve their element-specific bounds (Physical: 3,5,7,10; Lightning: 1,3,8,16; Fire: 2,4,8,14; Cold: 2,5,7,12)
5. Confirm percentage damage prefixes have explicit Vector2i(1, 32) (not relying on default)
6. Confirm SAVE_VERSION = 4 in save_manager.gd
7. Confirm disabled/commented suffixes are untouched
8. Confirm from_affix() in item_affixes.gd is unchanged
9. Confirm models/affixes/affix.gd is NOT modified (scaling formula is locked)
10. Print tier-32 and tier-1 values for each affix category to verify pre-prestige floor is preserved:
    - Flat damage Physical: tier-32 = 3-5 min, 7-10 max (matches old tier-8 floor)
    - % Damage: tier-32 = 2-10% (matches old tier-8 floor)
    - Defensive: tier-32 = same as old tier-30 floor (2-5 flat, 1-3%)
    - Resistance: tier-32 = 1-2% (lower than old tier-8 floor of 5-12%, but necessary for cap safety)
</verification>

<success_criteria>
- All 27 active affixes in item_affixes.gd have tier_range = Vector2i(1, 32)
- Resistance affixes retuned to base 1, 2 (tier-1 ceiling = 32-64%, tier-32 floor = 1-2%)
- Flat damage affixes preserve element spread ratios (Physical tight, Lightning wide, Fire wide, Cold moderate)
- SAVE_VERSION = 4 in save_manager.gd — old saves deleted on load
- No affix.quality() function added — is_item_better() unchanged
- models/affixes/affix.gd is NOT modified
- Disabled suffixes remain disabled
</success_criteria>

<output>
After completion, create `.planning/phases/37-affix-tier-expansion/37-01-SUMMARY.md`
</output>
