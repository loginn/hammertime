---
phase: 20-crafting-ux-enhancements
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [project.godot, scenes/forge_view.gd]
autonomous: true
requirements: [CRAFT-01, CRAFT-02]
gap_closure: true

must_haves:
  truths:
    - "Hovering a hammer button shows tooltip within 0.2 seconds"
    - "Melt/equip buttons remain functional after switching item types"
    - "Buttons correctly enable/disable based on current_item state after type switch"
  artifacts:
    - path: "project.godot"
      provides: "Reduced tooltip delay setting"
      contains: "timers/tooltip_delay_sec=0.2"
    - path: "scenes/forge_view.gd"
      provides: "Button state refresh on type switch"
      contains: "update_melt_equip_states"
  key_links:
    - from: "scenes/forge_view.gd"
      to: "_on_item_type_selected"
      via: "update_melt_equip_states() call"
      pattern: "update_melt_equip_states"
---

<objective>
Close two UAT gaps from Phase 20 verification:

1. (minor) Tooltip delay too slow — Godot default 0.5s feels sluggish. Reduce to 0.2s via project.godot setting.
2. (major) Melt/equip buttons stop working after first use when switching item types. Root cause: `_on_item_type_selected()` updates `current_item` but never refreshes button disabled states.

Purpose: Fix broken crafting workflow and improve tooltip responsiveness.
Output: Both files patched, buttons functional across type switches.
</objective>

<execution_context>
@/home/travelboi/.claude/get-shit-done/workflows/execute-plan.md
@/home/travelboi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-crafting-ux-enhancements/20-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce tooltip delay and fix melt/equip button states on type switch</name>
  <files>project.godot, scenes/forge_view.gd</files>
  <action>
  Two changes:

  1. In `project.godot`, add a `[gui]` section (before `[rendering]`) with the tooltip delay setting:
     ```
     [gui]

     timers/tooltip_delay_sec=0.2
     ```

  2. In `scenes/forge_view.gd`, in the `_on_item_type_selected()` function (line 271), add `update_melt_equip_states()` as the last call in the function, after `update_item_type_button_states()` on line 291. The function currently ends at line 292 — add the new call at line 293 so it reads:

     ```gdscript
     func _on_item_type_selected(item_type: String) -> void:
         # ... existing code ...
         update_current_item()
         update_item_type_button_states()
         update_melt_equip_states()
     ```

  This ensures that when the user switches item types, the melt/equip buttons correctly reflect whether the new type has an item (enabled) or not (disabled). Without this call, the buttons stay in whatever state they were left in by the previous melt/equip action.
  </action>
  <verify>
  1. Open `project.godot` and confirm `[gui]` section exists with `timers/tooltip_delay_sec=0.2`
  2. Open `scenes/forge_view.gd` and confirm `_on_item_type_selected()` ends with `update_melt_equip_states()` call
  3. Search for any syntax errors: look for mismatched indentation or missing colons
  </verify>
  <done>
  - project.godot contains `[gui]` section with `timers/tooltip_delay_sec=0.2`
  - `_on_item_type_selected()` calls `update_melt_equip_states()` after `update_item_type_button_states()`
  - Tooltips will appear in 0.2s instead of 0.5s
  - Melt/equip buttons will correctly enable/disable when switching between item types after melting or equipping
  </done>
</task>

</tasks>

<verification>
- Grep project.godot for `tooltip_delay_sec` — must find `0.2`
- Grep forge_view.gd `_on_item_type_selected` function body — must contain `update_melt_equip_states()`
- No other functions in forge_view.gd were modified
</verification>

<success_criteria>
- UAT Test 1 (Hammer Tooltips): Tooltip delay reduced from 0.5s to 0.2s
- UAT Test 4 (Direct Melt): Melt/equip buttons remain functional after melt/equip + type switch
- UAT Tests 5, 6 (Equip Overwrite Confirmation): Unblocked by Test 4 fix
</success_criteria>

<output>
After completion, create `.planning/phases/20-crafting-ux-enhancements/20-03-SUMMARY.md`
</output>
