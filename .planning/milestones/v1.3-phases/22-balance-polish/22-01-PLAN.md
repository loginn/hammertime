---
phase: 22-balance-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - autoloads/game_state.gd
  - models/monsters/biome_config.gd
  - scenes/forge_view.tscn
autonomous: true
requirements: [BAL-01, BAL-02, UI-01]

must_haves:
  truths:
    - "Fresh game starts with 1 Runic Hammer in currency inventory"
    - "Fresh game starts with LightSword base in crafting inventory (existing behavior preserved)"
    - "Forest biome monsters have ~40% reduced base HP and base damage compared to previous values"
    - "debug_hammers flag is set to false in game_state.gd"
    - "HeroStatsPanel text fits within its bounds at font size 11 or lower without overflow"
  artifacts:
    - path: "autoloads/game_state.gd"
      provides: "Starter Runic Hammer and debug_hammers=false"
      contains: "runic.*1"
    - path: "models/monsters/biome_config.gd"
      provides: "Reduced Forest monster base stats"
      contains: "Forest Bear"
    - path: "scenes/forge_view.tscn"
      provides: "HeroStatsLabel font size verified/adjusted"
      contains: "normal_font_size"
  key_links:
    - from: "autoloads/game_state.gd"
      to: "scenes/forge_view.gd"
      via: "GameState.currency_counts read by update_currency_button_states()"
      pattern: "currency_counts"
    - from: "models/monsters/biome_config.gd"
      to: "models/monsters/pack_generator.gd"
      via: "PackGenerator.create_pack reads monster_type.base_hp/base_damage"
      pattern: "monster_type.base_hp"
---

<objective>
Tune level 1 balance and polish UI for milestone v1.3 ship.

Purpose: Fresh heroes need to survive early content, and the UI must display all stats without overflow. This is the final phase before milestone completion.
Output: Balanced level 1 experience with starter gear, reduced monster stats, disabled debug mode, and verified stat panel sizing.
</objective>

<execution_context>
@/home/travelboi/.claude/get-shit-done/workflows/execute-plan.md
@/home/travelboi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@autoloads/game_state.gd
@models/monsters/biome_config.gd
@scenes/forge_view.tscn
@scenes/forge_view.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Starter gear, monster rebalance, and debug flag</name>
  <files>autoloads/game_state.gd, models/monsters/biome_config.gd</files>
  <action>
    **In autoloads/game_state.gd:**

    1. Set `debug_hammers` to `false` (line 3: change `var debug_hammers: bool = true` to `var debug_hammers: bool = false`)

    2. In `initialize_fresh_game()`, change the runic currency initialization from 0 to 1:
       Change `"runic": 0` to `"runic": 1` in the currency_counts dictionary.
       All other currencies remain at 0.

    **In models/monsters/biome_config.gd:**

    3. Reduce all Forest biome (first biome, levels 1-99) monster base stats by 40%. Update the MonsterType.create() calls:
       - Forest Bear: `MonsterType.create("Forest Bear", 72.0, 7.0, 0.8)` (was 120.0, 12.0)
       - Timber Wolf: `MonsterType.create("Timber Wolf", 48.0, 6.0, 1.2)` (was 80.0, 10.0)
       - Wild Boar: `MonsterType.create("Wild Boar", 60.0, 8.0, 0.9)` (was 100.0, 14.0)
       - Venomous Spider: `MonsterType.create("Venomous Spider", 30.0, 7.0, 1.8)` (was 50.0, 11.0)
       - Forest Sprite: `MonsterType.create("Forest Sprite", 24.0, 5.0, 2.0)` (was 40.0, 8.0)
       - Bramble Golem: `MonsterType.create("Bramble Golem", 90.0, 4.0, 0.6)` (was 150.0, 7.0)

    Note: Attack speeds are NOT changed (preserves monster type identity). Only HP and damage are reduced. Other biomes (Dark Forest, Cursed Woods, Shadow Realm) are NOT changed.
  </action>
  <verify>
    Read game_state.gd and confirm: debug_hammers = false, currency_counts["runic"] = 1 in initialize_fresh_game().
    Read biome_config.gd and confirm: Forest Bear base_hp = 72.0, base_damage = 7.0. All Forest monsters have reduced values. Dark Forest monsters unchanged.
  </verify>
  <done>
    - debug_hammers is false (no more 999 hammers on start)
    - Fresh game grants exactly 1 Runic Hammer
    - Forest monsters have ~40% reduced HP and damage
    - Other biome monsters are unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix stat panel text sizing</name>
  <files>scenes/forge_view.tscn</files>
  <action>
    **Verify HeroStatsPanel sizing in scenes/forge_view.tscn:**

    1. Check the HeroStatsLabel node dimensions:
       - Panel: offset_left=810, offset_top=230, offset_right=1240, offset_bottom=660 (430x430 panel)
       - Label: offset_left=10, offset_top=10, offset_right=420, offset_bottom=420 (410x410 content area)
       - Current font size: normal_font_size = 11

    2. Calculate max content height:
       - Worst case hero stats: "Hero Stats:" header + blank line + "Offense:" header + 3 offense lines + blank line + "Defense:" header + 3 defense type lines + 3 resistance lines = ~13 lines
       - At font size 11, line height ~16px: 13 * 16 = 208px fits in 410px
       - Stat comparison worst case: header + item names + blank + ~12 stat lines = ~15 lines = 240px, fits in 410px

    3. If the current sizing is adequate (410px height for ~15 max lines at 16px line height = 240px needed), leave font_size at 11.
       If content would exceed available height, reduce normal_font_size to 10.

    4. Verify scroll_active = false is set on HeroStatsLabel (already the case).

    The existing layout should be fine. This task confirms it and makes any adjustment if needed.
  </action>
  <verify>
    Read forge_view.tscn and confirm: HeroStatsLabel has bbcode_enabled = true, scroll_active = false, and normal_font_size is 11 (or 10 if adjusted). Panel dimensions provide adequate space for max stat content.
  </verify>
  <done>
    - HeroStatsPanel text fits within viewport bounds for all stat display scenarios
    - No scrollbar needed (scroll_active = false)
    - Font size is readable (11 or 10)
  </done>
</task>

</tasks>

<verification>
1. game_state.gd: debug_hammers = false, runic starts at 1
2. biome_config.gd: Forest monsters have reduced base stats, other biomes unchanged
3. forge_view.tscn: HeroStatsLabel fits all stat scenarios without overflow
4. No new files created, no scenes modified beyond forge_view.tscn (if needed)
</verification>

<success_criteria>
- BAL-01: New game starts with 1 Runic Hammer (verify currency_counts["runic"] = 1 in initialize_fresh_game)
- BAL-02: Forest monsters reduced ~40% HP/damage making level 1 survivable for fresh heroes
- UI-01: Stat panel text fits within HeroStatsPanel bounds at font size 11
- debug_hammers flag disabled for production
</success_criteria>

<output>
After completion, create `.planning/phases/22-balance-polish/22-01-SUMMARY.md`
</output>
