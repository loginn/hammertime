---
phase: 31-repo-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.gitignore]
autonomous: true
requirements: [REPO-01, REPO-02]

must_haves:
  truths:
    - "Running `git status` shows no .tmp files tracked or staged"
    - "The .gitignore file contains `*.tmp` and newly created .tmp files are not picked up by `git status`"
    - "The root directory contains no .tmp files on disk"
  artifacts:
    - path: ".gitignore"
      provides: "Updated ignore rules with *.tmp pattern"
      contains: "*.tmp"
  key_links:
    - from: ".gitignore"
      to: "git index"
      via: "git rm --cached removes tracking; .gitignore prevents re-add"
      pattern: "\\*.tmp"
---

<objective>
Remove 6 stale Godot editor .tmp files from git tracking and disk, and update .gitignore to prevent future .tmp files from being committed.

Purpose: Clean repository hygiene -- these are throwaway Godot editor recovery files that should never have been committed.
Output: Updated .gitignore, clean git index, no .tmp files on disk.
</objective>

<execution_context>
@/home/travelboi/.claude/get-shit-done/workflows/execute-plan.md
@/home/travelboi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .gitignore with tmp pattern</name>
  <files>.gitignore</files>
  <action>
Add `*.tmp` to the .gitignore file under the existing Godot ignores section. The current .gitignore contains:

```
# Godot 4+ specific ignores
.godot/
/android/
```

Add a blank line after `/android/`, then add:

```
# Godot editor temp files
*.tmp
```

This ensures any future .tmp files created by the Godot editor are automatically ignored.
  </action>
  <verify>
Run: `grep '*.tmp' .gitignore`
Expected: Line containing `*.tmp` is present.
  </verify>
  <done>The .gitignore file contains the `*.tmp` pattern and will prevent future .tmp files from being tracked.</done>
</task>

<task type="auto">
  <name>Task 2: Remove tracked .tmp files from git and disk</name>
  <files>node_2d.tscn14021954782.tmp, node_2d.tscn3087203284.tmp, node_2d.tscn3091396684.tmp, node_2d.tscn3103154948.tmp, node_2d.tscn3126370519.tmp, node_2d.tscn3150287910.tmp</files>
  <action>
Remove all 6 .tmp files from both git tracking and disk using:

```bash
git rm node_2d.tscn14021954782.tmp node_2d.tscn3087203284.tmp node_2d.tscn3091396684.tmp node_2d.tscn3103154948.tmp node_2d.tscn3126370519.tmp node_2d.tscn3150287910.tmp
```

This removes the files from the git index AND deletes them from disk in a single command. Then commit the .gitignore update and file removals together in one commit:

```bash
git add .gitignore
git commit -m "chore(31-01): remove .tmp files and update .gitignore"
```

After committing, verify clean state:
1. `git status` shows no .tmp files
2. `ls *.tmp 2>/dev/null` returns nothing
3. `touch test.tmp && git status` shows test.tmp is NOT listed (ignored), then `rm test.tmp`
  </action>
  <verify>
Run all three checks:
1. `git status` -- no .tmp files tracked or staged
2. `ls *.tmp 2>/dev/null` -- no output (files gone from disk)
3. `touch test.tmp && git check-ignore test.tmp && rm test.tmp` -- outputs "test.tmp" (confirming ignore works)
  </verify>
  <done>All 6 .tmp files are removed from git tracking and disk. New .tmp files are ignored by git.</done>
</task>

</tasks>

<verification>
1. `git status` shows a clean working tree with no .tmp references
2. `ls *.tmp 2>/dev/null` in the project root returns empty
3. `grep '*.tmp' .gitignore` returns the pattern
4. `touch test.tmp && git check-ignore test.tmp && rm test.tmp` confirms the ignore rule works
</verification>

<success_criteria>
- Zero .tmp files tracked by git (`git ls-files '*.tmp'` returns empty)
- Zero .tmp files on disk in project root
- .gitignore contains `*.tmp` pattern
- Newly created .tmp files are ignored by git
</success_criteria>

<output>
After completion, create `.planning/phases/31-repo-hygiene/31-01-SUMMARY.md`
</output>
