---
phase: 09-defensive-prefix-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - autoloads/tag.gd
  - models/affixes/affix.gd
  - autoloads/item_affixes.gd
  - models/stats/stat_calculator.gd
  - models/items/armor.gd
  - models/items/boots.gd
  - models/items/helmet.gd
autonomous: true

must_haves:
  truths:
    - "Non-weapon items (armor, boots, helmet) can roll defensive prefix affixes when crafted"
    - "Defensive prefixes use 30 tiers (T1=best, T30=worst) while existing offensive prefixes keep 8 tiers"
    - "Armor/boots/helmet gain armor values from flat + percentage armor affixes applied additively"
    - "Rings cannot roll defensive prefixes (no Tag.DEFENSE in valid_tags)"
    - "Utility prefixes (+Life, +Mana, +%Life) available for non-weapon items"
  artifacts:
    - path: "autoloads/tag.gd"
      provides: "New StatType enums and UTILITY/EVASION tags"
      contains: "PERCENT_ARMOR"
    - path: "models/affixes/affix.gd"
      provides: "Configurable tier_range per affix"
      contains: "tier_range"
    - path: "autoloads/item_affixes.gd"
      provides: "6 defensive prefixes + 3 utility prefixes"
      contains: "Armored"
    - path: "models/stats/stat_calculator.gd"
      provides: "Percentage stat calculation method"
      contains: "calculate_percentage_stat"
    - path: "models/items/armor.gd"
      provides: "Flat + percentage armor/evasion/ES calculation"
      contains: "PERCENT_ARMOR"
  key_links:
    - from: "autoloads/item_affixes.gd"
      to: "autoloads/tag.gd"
      via: "Tag.StatType enum values in affix definitions"
      pattern: "Tag\\.StatType\\.PERCENT_ARMOR"
    - from: "autoloads/item_affixes.gd"
      to: "models/affixes/affix.gd"
      via: "Vector2i tier_range parameter in Affix.new()"
      pattern: "Vector2i\\(1,\\s*30\\)"
    - from: "models/items/armor.gd"
      to: "models/stats/stat_calculator.gd"
      via: "calculate_percentage_stat() call for %armor"
      pattern: "calculate_percentage_stat"
    - from: "autoloads/item_affixes.gd"
      to: "models/items/item.gd"
      via: "Affixes.from_affix() copies tier_range for correct tier rolling"
      pattern: "from_affix"
---

<objective>
Add defensive and utility prefix affixes to the data layer: extend Tag enums, make Affix tier range configurable, create 9 new prefixes (6 defensive + 3 utility), add percentage stat calculation to StatCalculator, and update armor/boots/helmet models to calculate flat + percentage defense stats.

Purpose: Non-weapon items currently have zero valid prefixes (all prefixes require Tag.WEAPON). This plan creates the complete data infrastructure so defensive items can roll meaningful prefix affixes.

Output: Tag enums expanded, Affix supports configurable tier ranges (30 tiers for defensive, 8 for existing), 9 new prefixes in ItemAffixes, StatCalculator handles percentage stats, armor/boots/helmet calculate defense from affixes.
</objective>

<execution_context>
@/home/travelboi/.claude/get-shit-done/workflows/execute-plan.md
@/home/travelboi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-defensive-prefix-foundation/09-CONTEXT.md
@.planning/phases/09-defensive-prefix-foundation/09-RESEARCH.md
@autoloads/tag.gd
@models/affixes/affix.gd
@autoloads/item_affixes.gd
@models/stats/stat_calculator.gd
@models/items/armor.gd
@models/items/boots.gd
@models/items/helmet.gd
@models/items/basic_armor.gd
@models/items/basic_boots.gd
@models/items/basic_helmet.gd
@models/items/basic_ring.gd
@models/items/item.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Tag enums, make Affix tier range configurable, add defensive + utility prefixes</name>
  <files>
    autoloads/tag.gd
    models/affixes/affix.gd
    autoloads/item_affixes.gd
  </files>
  <action>
**autoloads/tag.gd:**
Add a UTILITY tag constant (alongside existing DEFENSE, ARMOR, ENERGY_SHIELD, MANA constants). Add EVASION tag constant if not already present.

Extend the StatType enum with these new values (append after MOVEMENT_SPEED):
- PERCENT_ARMOR
- PERCENT_EVASION
- PERCENT_ENERGY_SHIELD
- PERCENT_HEALTH
- FLAT_EVASION

Keep all existing enum values in their current positions to avoid breaking existing code that relies on enum integer values.

**models/affixes/affix.gd:**
Add a `var tier_range: Vector2i = Vector2i(1, 8)` property (default preserves existing 8-tier behavior for all current affixes).

Add `var base_min: int = 0` and `var base_max: int = 0` properties to store original base values before tier scaling (needed for future tier info display).

Update `_init()` to accept an optional 7th parameter `p_tier_range: Vector2i = Vector2i(1, 8)`. Inside _init():
- Store p_tier_range in self.tier_range
- Store p_min in self.base_min, p_max in self.base_max
- Replace `self.tier = randi_range(1, 8)` with `self.tier = randi_range(tier_range.x, tier_range.y)`
- Replace `self.min_value = p_min * (9 - tier)` with `self.min_value = p_min * (tier_range.y + 1 - tier)`
- Replace `self.max_value = p_max * (9 - tier)` with `self.max_value = p_max * (tier_range.y + 1 - tier)`
- self.value = randi_range(self.min_value, self.max_value) stays the same

Update `Affixes.from_affix()` in item_affixes.gd to pass the template's tier_range when creating copies:
```
static func from_affix(template: Affix) -> Affix:
    var affix_copy = Affix.new(
        template.affix_name,
        template.type,
        template.base_min,    # Use base_min/base_max, not min_value/max_value
        template.base_max,    # (those are tier-scaled values)
        template.tags,
        template.stat_types,
        template.tier_range   # Pass tier_range so copy uses correct range
    )
    return affix_copy
```
CRITICAL: from_affix currently passes template.min_value and template.max_value, which are ALREADY tier-scaled. This means copies get double-scaled values. Fix this by using base_min/base_max instead. This is a bug fix for existing affixes too -- currently the from_affix creates affixes with pre-scaled min/max that get scaled again. With the new base_min/base_max properties, pass those instead.

**autoloads/item_affixes.gd:**
Add 9 new prefixes to the prefixes array (after existing weapon prefixes). All defensive/utility prefixes use Vector2i(1, 30) for tier_range.

Defensive prefixes (per user decision: current basic items are armor bases, so armor prefixes only):
1. "Armored" - PREFIX, base 2-5, tags [Tag.DEFENSE, Tag.ARMOR], stat_types [Tag.StatType.FLAT_ARMOR], tier_range Vector2i(1, 30)
2. "Reinforced" - PREFIX, base 1-3, tags [Tag.DEFENSE, Tag.ARMOR], stat_types [Tag.StatType.PERCENT_ARMOR], tier_range Vector2i(1, 30)
3. "Evasive" - PREFIX, base 2-5, tags [Tag.DEFENSE, Tag.EVASION], stat_types [Tag.StatType.FLAT_EVASION], tier_range Vector2i(1, 30)
4. "Swift" - PREFIX, base 1-3, tags [Tag.DEFENSE, Tag.EVASION], stat_types [Tag.StatType.PERCENT_EVASION], tier_range Vector2i(1, 30)
5. "Warded" - PREFIX, base 3-6, tags [Tag.DEFENSE, Tag.ENERGY_SHIELD], stat_types [Tag.StatType.FLAT_ENERGY_SHIELD], tier_range Vector2i(1, 30)
6. "Arcane" - PREFIX, base 1-3, tags [Tag.DEFENSE, Tag.ENERGY_SHIELD], stat_types [Tag.StatType.PERCENT_ENERGY_SHIELD], tier_range Vector2i(1, 30)

Utility prefixes (Claude's discretion: non-weapons only, use Tag.DEFENSE so rings don't roll them):
7. "Healthy" - PREFIX, base 3-8, tags [Tag.DEFENSE, Tag.UTILITY], stat_types [Tag.StatType.FLAT_HEALTH], tier_range Vector2i(1, 30)
8. "Vital" - PREFIX, base 1-3, tags [Tag.DEFENSE, Tag.UTILITY], stat_types [Tag.StatType.PERCENT_HEALTH], tier_range Vector2i(1, 30)
9. "Mystic" - PREFIX, base 2-6, tags [Tag.DEFENSE, Tag.MANA, Tag.UTILITY], stat_types [Tag.StatType.FLAT_MANA], tier_range Vector2i(1, 30)

Existing weapon prefixes keep no tier_range argument (defaults to Vector2i(1, 8), preserving current 8-tier behavior). DO NOT expand offensive tiers to 30 -- that is explicitly deferred.

Note: BasicRing valid_tags are [Tag.ATTACK, Tag.CRITICAL, Tag.SPEED] -- no Tag.DEFENSE, so rings correctly cannot roll any defensive or utility prefix. BasicArmor has [Tag.DEFENSE, Tag.ARMOR, Tag.ENERGY_SHIELD], so it matches "Armored", "Reinforced", "Warded", "Arcane", "Healthy", "Vital", "Mystic" prefixes. BasicBoots adds Tag.SPEED. BasicHelmet adds Tag.MANA. All three match the utility prefixes.
  </action>
  <verify>
Open project in Godot editor (or run scene). Create a BasicArmor, upgrade to Magic with Runic Hammer, verify it can roll a prefix (should see "Armored", "Reinforced", "Warded", "Arcane", "Healthy", "Vital", or "Mystic"). Verify BasicRing still only rolls weapon-tagged prefixes (Physical Damage, %Physical Damage, etc.). Verify no GDScript errors in Output panel. Print ItemAffixes.prefixes.size() -- should be 18 (9 existing + 9 new).
  </verify>
  <done>
Tag.gd has UTILITY, EVASION constants and 5 new StatType enum values. Affix.gd has configurable tier_range with Vector2i default. ItemAffixes has 18 prefixes (9 weapon + 6 defensive + 3 utility). from_affix correctly propagates tier_range and uses base_min/base_max. Defensive prefixes use 30 tiers, weapon prefixes use 8 tiers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add percentage stat calculation and update defense item models</name>
  <files>
    models/stats/stat_calculator.gd
    models/items/armor.gd
    models/items/boots.gd
    models/items/helmet.gd
  </files>
  <action>
**models/stats/stat_calculator.gd:**
Add a new static method `calculate_percentage_stat()` after the existing `calculate_flat_stat()`:

```gdscript
## Calculates percentage-based stat modifiers using additive stacking.
## All "increased X%" affixes for a stat type sum, then apply once to base value.
## Matches INCREASED_DAMAGE pattern from calculate_dps().
## Example: base=100, two +50% affixes -> 100 * (1.0 + 0.5 + 0.5) = 200
static func calculate_percentage_stat(base_value: float, affixes: Array, stat_type: int) -> float:
    var additive_mult := 0.0
    for affix: Affix in affixes:
        if stat_type in affix.stat_types:
            additive_mult += affix.value / 100.0
    return base_value * (1.0 + additive_mult)
```

**models/items/armor.gd:**
Add `var base_evasion: int = 0` and `var original_base_evasion: int = 0` properties.

Update `update_value()` to apply percentage modifiers after flat additions for armor, evasion, energy shield, and health:
1. Flat armor -> then PERCENT_ARMOR applied to (original + flat)
2. Flat evasion -> then PERCENT_EVASION applied
3. Flat ES -> then PERCENT_ENERGY_SHIELD applied
4. Flat health -> then PERCENT_HEALTH applied
5. total_defense = base_armor (backward compat)

Update `get_display_text()` to show evasion if non-zero (alongside existing armor/ES/health display).

**models/items/boots.gd:**
Add `var base_evasion: int = 0` and `var original_base_evasion: int = 0` properties.
Add `var base_health: int = 0` and `var original_base_health: int = 0` properties.

Update `update_value()` same pattern as Armor: flat + percentage for armor, evasion, ES, movement speed, and health. total_defense = base_armor.

Update `get_display_text()` to show evasion and health if non-zero.

**models/items/helmet.gd:**
Add `var base_evasion: int = 0` and `var original_base_evasion: int = 0` properties.
Add `var base_health: int = 0` and `var original_base_health: int = 0` properties.

Update `update_value()` same pattern: flat + percentage for armor, evasion, ES, mana, and health. total_defense = base_armor.

Update `get_display_text()` to show evasion and health if non-zero.

**Key implementation note:** All current basic items have original_base_evasion = 0 (they are armor bases per user decision). The evasion/ES calculation infrastructure exists for future base types (leather armor, mage robes) that will use Tag.EVASION/Tag.ENERGY_SHIELD tags instead of Tag.ARMOR. For now, only armor prefixes ("Armored", "Reinforced") will actually roll on these items because their valid_tags include Tag.ARMOR but not Tag.EVASION.

**Percentage calculation order:** Always flat first, then percentage applied to (original_base + flat). This matches the DPS calculation pattern in StatCalculator.calculate_dps() where FLAT_DAMAGE adds first, then INCREASED_DAMAGE multiplies.
  </action>
  <verify>
Create a BasicArmor, upgrade to Rare (Forge Hammer), apply Grand Hammer to add prefixes. If "Armored" prefix rolls, verify base_armor increases above original 15. If "Reinforced" prefix rolls alongside "Armored", verify the percentage is applied additively to (15 + flat_armor). Print armor.base_armor, armor.base_evasion, armor.base_energy_shield after update_value() -- evasion should be 0 (no evasion affixes can roll on armor-tagged items). No GDScript errors.
  </verify>
  <done>
StatCalculator has calculate_percentage_stat() using additive stacking. Armor, Boots, and Helmet update_value() methods apply flat + percentage for all defense types (armor, evasion, ES) plus health. All three item types have evasion properties (default 0) for future base type support. total_defense backward compatible (equals base_armor).
  </done>
</task>

</tasks>

<verification>
1. Create BasicArmor -> Runic Hammer (upgrade to Magic) -> verify a defensive/utility prefix appears
2. Create BasicBoots -> Runic Hammer -> verify prefix appears
3. Create BasicHelmet -> Runic Hammer -> verify prefix appears
4. Create BasicRing -> Runic Hammer -> verify only weapon prefixes roll (no defensive/utility)
5. Create BasicArmor -> Forge Hammer (upgrade to Rare) -> Grand Hammer x3 -> verify up to 3 prefixes, including possible stacking of flat armor + % armor
6. Verify item.display() shows updated defense values after crafting
7. No GDScript warnings or errors in Output panel
</verification>

<success_criteria>
- 9 new prefixes in ItemAffixes (6 defensive + 3 utility), all using 30-tier range
- Existing 9 weapon prefixes unchanged (8-tier range)
- Affix tier_range configurable per affix via constructor parameter
- StatCalculator.calculate_percentage_stat() works with additive stacking
- Armor/Boots/Helmet update_value() applies flat + percentage for all defense types
- Rings cannot roll defensive or utility prefixes
- No regressions to existing weapon crafting
</success_criteria>

<output>
After completion, create `.planning/phases/09-defensive-prefix-foundation/09-01-SUMMARY.md`
</output>
